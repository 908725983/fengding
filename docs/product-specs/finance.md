# 资金领域规格

## 需求来源与事实边界

- 直接业务来源：[07-资金模块.md](../references/requirements/07-资金模块.md)；业务闭环、全局交互与角色资料来自 [01-整体架构与首页.md](../references/requirements/01-整体架构与首页.md) §2、§4～§5。
- 功能切片、章节、依赖、规则归属和字段准备度以 [产品规格索引](index.md) 为唯一目录。
- `FIN-01～FIN-11` 直接整理自原文 §10。“不变量”中的整数分、原子流水和幂等属于 Harness 工程约束。
- 原文没有明确应收/应付形成时点，退款与退货入库的跨域账务关系也不完整，应付账龄区间和逾期边界没有完整定义；应收集成按 `DEC-FIN-001～005` 处理，供应商应付/付款/核销由 `FIN-002` 的 `DEC-FIN-041～054` 继续门控。

## 目标与范围

跟踪客户应收、供应商应付、收付款、核销和资金账户，使业务单据与资金变化能够相互追溯。

## 主要页面

- 应收/应付列表、按单据/商品明细、账龄分析。
- 核销单列表、新增与明细。
- 收款、付款、退款确认、拉新提现。
- 资金账户、收支明细/汇总、结转、银行和在线支付设置。
- 内部转账、其他收支、机构收款和资金统计。

## 业务规则

| ID | 必须行为 |
|---|---|
| FIN-01 | 收付款与应收应付单据进行匹配核销。 |
| FIN-02 | 按单据日期自动计算账龄区间。 |
| FIN-03 | 每笔确认收付款实时更新资金账户余额。 |
| FIN-04 | 已核销收付款不可删除或修改金额。 |
| FIN-05 | 退款优先原路；特殊情况可选择现金或余额。 |
| FIN-06 | 已资金结转月份禁止资金出入。 |
| FIN-07 | 非业务收付款必须财务审核。 |
| FIN-08 | 转账只允许内部资金账户之间调拨。 |
| FIN-09 | 支持第三方支付通道开通配置。 |
| FIN-10 | 拉新奖励提现审核后打款。 |
| FIN-11 | 多机构模式按机构汇总收款。 |

## 不变量

- 金额为整数分；收付款、已核销、未核销三者关系必须精确且不可为负。
- 一次核销可匹配多张单据，但总核销额不得超过可用收付款额或单据未核销额。
- 账户余额变化和资金流水作为一个结果，重复请求不得重复入账。
- 原始业务单据、收付款与核销单之间可双向追溯。

## P0 验收

1. 收款可分配到多张应收，部分核销后余额正确；超额核销被拒绝。
2. 已核销收款不能删除或改金额，未核销草稿按权限可操作。
3. 确认收款只更新一次账户余额并生成流水。
4. `DEC-FIN-004` 明确起算日、应付区间和逾期规则后，账龄在模拟日期边界前后进入正确区间。
5. 结转月份的收付款、退款与转账均被拦截。
6. 退款显示原支付方式，改用其他方式时记录原因。

## 原型模拟边界

不接入微信、支付宝、银联、银行或真实打款；支付设置只展示配置流程，所有支付结果标记为模拟。

## ORD-004 到 FIN-001 的应收集成边界

当前状态：`ORD-004` 集成契约为 `ready`；完整 `FIN-001` 契约也已在 2026-08-14 完成决策写回并达到 `ready`。订单级确认发货形成一次应收，金额为订单保存的完整 `orderAmountCents`；不按出库单分摊整单优惠或运费，预收保持占用直至 `FIN-001` 收款/核销，签收不再形成资金结果。

- 资金领域拥有应收事实；订单只能通过公开幂等 command 请求形成应收，不能在订单聚合中复制第二套可编辑应收单。
- command 至少需要 `requestId/orderId/customerId/orderNo/orderAmountCents/occurredAt` 和订单/客户金额快照；在订单级确认发货时调用，客户自提的确认发货同样代表确认交付。
- 发货状态、订单全额应收记录和订单活动日志必须原子成功或失败；资金 provider unavailable/error 时不能先显示已发货再补账。未发货出库作废不碰应收，签收只完成履约。
- `FIN-001` 页面未完成期间可以使用明确标识的确定性 fake adapter 证明调用、幂等和回滚，但不得声称应收列表、收款、核销或资金账户已经实现。
- 差异在补发完成、忽略短装或选择短装退款后允许发货。短装退款由 `ORD-005` 的窄 Finance 契约处理：不生成退货入库，发货时先形成完整原应收并在同一回滚边界追加差异贷项；不能只记录“退款成功”或改写原应收。

## FIN-003 字段与交互契约

当前状态：`ready`，实现状态：`passing`。用户于 2026-08-14 确认 `DEC-FIN-006～016` 全部推荐方案；账户生命周期、期间口径、期初、负余额、结转/反结转、银行关系/隐私、支付模拟和角色权限均按下述结论实现。领域检查点为 `1589846`，Runtime/UI 检查点为 `64351b4`；全仓自动验证和浏览器黄金路径均已通过。

### 事实来源

| 来源 | 已确认事实 | 已确认边界或关联决策 |
|---|---|---|
| `07-资金模块.md §1.1、§5.1～§5.3` | 资金账户含现金/银行/微信/支付宝；列表显示期初、本期收支、当前余额、状态；可查看明细/编辑；流水关联单据并保存操作后余额 | 新增/编辑字段、期间口径、停用/删除、期初更正、余额不足 |
| `07-资金模块.md §5.4～§5.5` | 其他收付款有明细和按项目汇总 | 其单据来自 `FIN-004`，本切片不创建其他收支或伪造汇总 |
| `07-资金模块.md §5.6、FIN-06` | 月份卡片展示未结转/已结转；期末转下期期初；结转月禁止资金出入；原文提到反结转 | 首月、顺序、未来/空月、反结转条件、原子范围 |
| `07-资金模块.md §5.7` | 银行名称、账户户名、账号、开户行、对公/对私、当前余额、启停状态 | 与银行类型资金账户的关系、CRUD、唯一性、账号遮蔽 |
| `07-资金模块.md §5.8、FIN-09` | 展示微企付/银联商务、未开通/已开通及开通向导 | 外部申请字段、审批状态和本地原型能否变为已开通 |
| `07-资金模块.md §10 FIN-03/06` | 确认收付款实时更新余额，结转月锁定资金出入 | 具体收付款属于 `FIN-001/002/004`，本切片只提供公开账户命令/期间门 |
| `01-整体架构与首页.md §4～§5`、`08-设置模块.md §3.1.3` | 列表/详情通用交互；管理员和财务可写资金，销售主管只读；存在查看/管理账户、查看流水、管理银行/支付细权限 | 结转细权限和敏感账号 baseline 映射 |

### 页面与导航

本切片把资金入口从规划页升级为账户工作区；路由是工程组织，不新增业务含义：

| 页面 | 路由 | 内容与边界 |
|---|---|---|
| 资金账户列表 | `/finance/accounts`（`/finance` 重定向） | 原文八列、选择月份、本期汇总和允许时的账户管理 |
| 账户详情 | `/finance/accounts/:accountId` | 账户基础信息、期间金额、逐笔收支和操作后余额；关联单据仅链接已实现来源 |
| 账户收支汇总 | `/finance/account-summary` | 按选择月份逐账户显示期初/收入/支出/期末；不是 `FIN-005` 全局资金统计 |
| 资金结转 | `/finance/carryovers` | 年份、12 月卡片、结转状态、允许时结转/反结转和审计摘要 |
| 银行列表 | `/finance/banks` | 原文七列和允许时的资料维护；不连接真实银行余额 |
| 在线支付设置 | `/finance/payment-channels` | 原文通道介绍、固定通道与明确模拟申请；不接第三方接口 |

- “其他收付款明细/汇总”显示为等待 `FIN-004` 的明确 unavailable 说明，不创建成功按钮或虚构记录。
- 收付款、核销、转账、退款和资金统计仍保持后续切片边界；账户详情不能借链接提前生成这些单据页面。
- 资金工作区使用二级导航；页面动作按 `DEC-FIN-016` 使用统一四层权限控制。

### 筛选与列表

原文没有为资金账户、银行和结转定义关键字等筛选条件，当前不自行添加业务筛选。所有普通列表沿用全局默认 30 条/页和 `createdAt desc,id desc` 稳定排序；月份属于金额口径而非搜索条件。“当期”采用 Asia/Shanghai 受控时钟所在自然月，页面可切换已存在期间并写入 URL，默认当前月且未来月份不可选。

资金账户列表：

| 列 | 数据契约 | 显示规则 |
|---|---|---|
| 账户名称 | `name` | 链接详情；空值不允许 |
| 账户类型 | `cash/bank/wechat/alipay` | 分别显示现金、银行账户、微信、支付宝，不增加其他类型 |
| 期初余额 | `openingBalanceCents` | 选择期间期初，整数分格式化；首期取账户期初，后续取上一已结转期间期末 |
| 本期收入/支出 | `periodIncomeCents/periodExpenseCents` | 只汇总选择期间的不可变流水；均不得为负 |
| 当前/期末余额 | 计算值 | `期初+本期收入-本期支出`；不得从银行接口读取 |
| 状态 | `enabled/disabled` | 停用后不可用于新资金动作，但历史、余额和汇总继续展示；可重新启用 |
| 操作 | 查看明细、编辑 | 无权限或不合法状态不显示假入口 |

账户收支明细按 `occurredAt asc,id asc` 计算同时间稳定余额，页面默认展示选择期间，列固定为日期、账户名称、单据类型、单据号、往来单位、收入、支出、操作后余额和摘要。单条只能有收入或支出一侧大于 0；两者同时为 0 或同时大于 0 都是非法数据。未知/未接入来源显示“来源尚未接入”，不能生成可点击死链接。

账户收支汇总列固定为账户名称、类型、期初、收入、支出和期末。合计基于完整选择期间结果，不只当前页；销售主管拥有只读账户/流水权限，但银行账号只显示后四位。

银行列表列固定为银行名称、账户名称、银行账号、开户行、账户类型、当前余额和状态。银行资料必须一对一关联一个既有 `bank` 类型资金账户；当前余额取关联账户的同一投影，不保存第二份可编辑余额，也不因维护银行资料自动创建账户或改变余额。

### 表单与校验

账户管理允许新增，字段为名称、现金/银行/微信/支付宝类型、启用状态、启用月份和首期期初余额。名称企业内唯一；已有流水或结转后类型不可改；不提供物理删除。除此之外还必须满足：

- 金额输入解析为整数分，最多两位小数；空值、非数字和超出安全整数拒绝，不用浮点累计。
- 账户类型只允许原文四类；名称、状态与期初不能通过页面临时数组保存。
- 期初余额不是普通资金流水；仅在账户没有任何流水且首月未结转前可改，之后更正等待 `FIN-004` 的可追溯调整单，不能直接重算历史。
- 账户写操作携带 `requestId/expectedUpdatedAt/operatorId/occurredAt`，幂等重放返回原结果，版本冲突要求重新加载。

银行资料允许新增、编辑和停用，固定包含关联银行资金账户、银行名称、账户户名、银行账号、开户行、对公/对私和启用/停用。账号去空格后企业内唯一且 4～64 字符；不臆造银行卡校验算法，不提供物理删除。停用后不可用于新业务。银行账号按不可信敏感输入处理：页面不得写入 URL、日志或错误详情；仅管理员/财务查看完整值，销售主管只见后四位。

支付通道固定展示微企付和银联商务，不接收真实商户号、密钥、证书、银行卡、身份证或支付凭据。开通动作只创建明确标识“原型模拟”的本地申请，显示“模拟申请中”但通道仍为未开通；baseline 可显式展示已开通，不得发起网络请求、创建支付账户或改变余额。

### 持久化对象与公开边界

最终 schema 名称可以按代码风格调整，但不得省略以下事实：

- `FundAccount`：ID、企业范围、名称、四类账户类型、状态、启用月份、首期期初整数分、创建/更新时间、版本和操作者；不保存可被页面任意覆盖的“当前余额”。
- `FundMovement`：ID、账户、方向、金额整数分、发生时间、来源类型/来源 ID/单号快照、往来单位快照、摘要、操作后余额、requestId 和操作者；确认后不可编辑删除。
- `FinancePeriod`：`YYYY-MM`、状态、各账户期初/收入/支出/期末快照、结转/反结转审计和版本；从账套启用月开始连续结转。
- `BankProfile`：ID、唯一关联的银行类型资金账户、银行/户名/账号/网点/对公私/状态、版本和审计；不复制余额。
- `PaymentChannel` 与模拟申请：固定通道资料、真实开通状态及明确模拟记录；不得保存真实凭据。
- 所有账户聚合和结转请求保留幂等记录；fixture 使用明显虚构银行、账号和摘要。

资金领域未来向 `FIN-001/002/004` 暴露公开 command：校验账户启用、权限、发生月份未锁定、金额、余额不为负和幂等，然后原子追加流水并返回余额；不得允许其他领域直接修改账户 Repository。本原型不提供透支配置，任何扣减导致负余额都整体拒绝。查询 provider 必须区分 `available/unavailable/error`，不能把未知余额显示为 0。

### 详情与操作矩阵

| 操作 | 允许角色/状态 | 前置校验 | 成功结果 | 失败结果 |
|---|---|---|---|---|
| 新增账户 | 管理员/财务 | 名称唯一、四类账户、启用月合法、期初为整数分 | 创建可追溯账户，不自动生成流水 | 保留输入，不创建半账户 |
| 编辑账户 | 管理员/财务；任意启停状态 | 乐观版本；已有流水/结转后类型与期初锁定 | 更新允许字段并记录审计 | `CONFLICT` 或规则错误，原记录不变 |
| 停用/恢复 | 管理员/财务 | 乐观版本 | 停用禁止新资金动作但保留历史/余额；恢复后可重新使用 | 不改余额或历史 |
| 查看账户/流水 | 管理员、财务、销售主管 | 账户可见；销售主管只读 | 显示期间汇总、逐笔余额和来源 | unavailable/error 不显示 0 |
| 结转月份 | 管理员/财务；目标月未结转 | 连续月份、非未来月、版本及全部账户可汇总 | 原子保存全部期末、生成下期初并锁定月份 | 失败整体回滚，可按 requestId 重试 |
| 反结转 | 管理员/财务；最新已结转月 | 原因 1～200 字、无更晚结转和后续流水 | 原子解除锁定、移除派生期初并留审计 | 不做级联重算，现场不变 |
| 新增/编辑/停用银行资料 | 管理员/财务 | 一对一 bank 账户、账号唯一 4～64 字、版本合法 | 资料更新，不改变资金余额 | 不创建/修改关联账户或余额 |
| 提交支付模拟申请 | 管理员/财务；通道未开通 | 固定通道、无真实凭据、requestId 幂等 | 新增“模拟申请中”记录，通道仍未开通 | 不发网络请求、不创建账户/余额 |

### 状态与计算

已由原文固定的计算：

```text
期间期末余额 = 期间期初余额 + 期间收入 - 期间支出
下期初余额 = 已结转上期期末余额
```

- 所有金额为整数分；逐笔操作后余额按确定顺序累计，汇总必须能与最后一笔余额和期间快照对账。
- 只有确认后的未来收款/付款/转账/退款/其他收支才能写资金流水；本切片不把草稿或模拟支付申请计入余额。
- 账套保存启用月，只能从该月开始按月连续结转，未来月不可结转，空月可结转。一次请求原子冻结当月全部账户期末并生成下月期初，重复 requestId 幂等；对应月份的资金写 command 必须在 Service 边界拒绝。
- 只允许反结转最新已结转月且原因 1～200 字；存在更晚结转或其后任何资金流水时拒绝。成功原子移除派生下月期初、解除锁定并保留不可变审计。
- 账户停用不等于余额归零或删除历史；允许带余额停用和恢复，但停用期间不能承接新资金动作。
- 当前只支持一种账面币种还是需要币种字段，原文没有给多币种或汇率；本切片不增加多币种换算。若后续出现真实多币种需求，必须新建决策而不能重解释现有金额。

### 权限与字段遮蔽

细权限固定为 `finance.view-accounts`、`finance.manage-accounts`、`finance.view-ledger`、`finance.carryover`、`finance.manage-banks`、`finance.manage-payment-channels`。管理员和财务拥有全部；销售主管只有账户/流水只读且银行账号遮蔽；业务员和仓库不可进入资金账户区域。

- 路由、UI、Runtime/Store、Service command 和导出（若后续加入）必须消费同一权限结果。
- 直接调用 Service 越权时拒绝；页面隐藏按钮不能代替命令授权。
- 银行账号、未来支付凭据和内部诊断信息不得出现在普通日志、错误或 URL。
- 本切片不因为财务可管理账户就自动赋予订单、库存、设置或真实支付权限。

### 页面状态与 Mock

单一 baseline 至少准备：现金、银行、微信/支付宝账户类型；启用/停用状态；有期初和多笔收入/支出的期间；一个已结转月和当前未结转月；关联银行资料；未开通支付通道。所有名称、账号、单号和往来单位均为明显虚构数据，并与未来切片来源明确区分。

| 场景 | 可见表现 | 数据结果/恢复 |
|---|---|---|
| `normal` | 账户、流水、汇总、结转、银行、支付介绍可达 | 余额和期间快照一致，写动作按确认契约执行 |
| `empty` | 各页显示成功空状态；无账户时不能结转 | 返回 normal 或清空筛选可恢复 |
| `error` | Repository/写入失败显示可理解错误和重试 | 不留半账户、半结转、半银行或余额变化 |
| `slow` | 提交中禁重复，旧响应不覆盖新版本 | 同 requestId 最多生效一次 |
| `permission-denied` | 无权限角色被路由拒绝且无写入口 | 直接 Service 调用同样拒绝 |
| `partial-failure` | 银行/支付资料失败不抹去账户汇总；余额/期间 provider 失败阻断相关动作 | unavailable/error 不显示 0 或已开通 |
| `boundary` | 金额两位小数、重名、非法类型、锁定月、未来月、停用账户、账号边界 | 校验失败前后状态哈希一致 |
| `concurrent` | 两会话编辑同账户或同时结转/反结转 | 首个成功，后续 `CONFLICT` 并重新加载 |

### 验收追踪

| 验收 ID | 来源/决策 | 可观察结果 | 自动证据 | 人工证据 |
|---|---|---|---|---|
| `FIN003-A01` | §5.1、`DEC-FIN-006/008` | 账户列表/表单/详情与生命周期不改写历史余额 | schema/repository/lifecycle 测试 | 新增、编辑、停用/恢复及详情检查 |
| `FIN003-A02` | §5.2～§5.3、FIN-03、`DEC-FIN-007/009` | 逐笔余额、期间汇总与余额规则精确一致 | integer-cents/ledger/summary 测试 | 账户明细与汇总对账 |
| `FIN003-A03` | §5.6、FIN-06、`DEC-FIN-010/011` | 结转/反结转原子、幂等、连续且锁定真实生效 | period/lock/idempotency/concurrency 测试 | 月份卡片、锁定和恢复检查 |
| `FIN003-A04` | §5.7、`DEC-FIN-012～014` | 银行资料不复制余额，关联/生命周期/账号遮蔽一致 | bank relation/validation/masking 测试 | 银行列表、表单及逐角色检查 |
| `FIN003-A05` | §5.8、FIN-09、`DEC-FIN-015` | 支付页面明确模拟且无真实请求、凭据或余额副作用 | fake-adapter/no-network 测试 | 通道介绍与模拟申请检查 |
| `FIN003-A06` | 权限树、`DEC-FIN-016` | 五角色路由/UI/Store/Service 一致 | permission matrix/bypass 测试 | 切换角色核对页面与动作 |
| `FIN003-A07` | Harness 可靠性/安全 | 八类场景、Mock 重置、1280px、键盘、控制台和干净重启通过 | 全仓验证、静态检查、双哈希 | 黄金旅程与截图 |

以上 11 组决策已由用户于 2026-08-14 回复“FIN-003 全部按推荐方案执行”确认并写回；`FIN-003` 已完成代码、Mock、页面与验收并达到 `passing`。后续 `FIN-001/002/004/005` 只能消费本切片 `public.ts` 的账户流水命令和查询边界，不得直接修改资金 Repository。

## FIN-001 字段与交互契约

当前状态：`ready`。`FIN-003`、`ORD-004` 已 passing，`DEC-FIN-001/004/005/017～030` 已由用户于 2026-08-14 确认推荐方案并逐项写回。订单确认发货时按完整订单金额只形成一次应收；账龄、唯一所有者、收款入账、预收、优惠、核销/取消、编号、日期、权限、隐私和附件均按本节“已确认方案”实施。`ready` 只表示允许编码，不代表实现已经完成。

### 事实来源

| 来源 | 已确认事实 | 本切片边界或采用的决策 |
|---|---|---|
| `07-资金模块.md §2.1～§2.4` | 客户应收汇总、按单据/商品明细、账龄区间、关键字/日期筛选和导出 | 生命周期、商品状态和账龄采用 `DEC-FIN-004/018/025` |
| `07-资金模块.md §3、FIN-01/04` | 收款与应收匹配；核销单/明细可追溯；一次核销可关联收款和应收；有取消入口；已核销收款不可删改金额 | 多对多、取消、作废和预收采用 `DEC-FIN-021/022/024` |
| `07-资金模块.md §4.1～§4.2、FIN-03` | 收款单字段、正常/已作废、收款方式、账户、立即核销、凭证和备注；每笔收款实时更新余额 | 确认时点、账户映射、余额方式和附件采用 `DEC-FIN-019/020/030` |
| `02-订单模块.md §2.3.7～§2.3.8` | 订单详情显示应收/已结算/待收、多条收款记录；支持从订单发起收款并填写优惠 | 手工优惠采用 `DEC-FIN-023`；自动计算明确 unavailable |
| `DEC-FIN-001/005`、`ORD-004` passing | 确认发货原子形成一次订单全额应收；签收不重复形成；预收保持占用等待 FIN-001 核销 | Finance canonical owner 和 Order public command 采用 `DEC-FIN-017` |
| `CUS-001` | 客户 ID/编码/名称/联系人/电话、信用额度、现结/月结/账期及 1～365 天账期已经可查询 | 快照和敏感字段可见范围采用 `DEC-FIN-017/029` |
| `FIN-003` passing | 四类启用账户、不可变资金流水、负余额规则、自然月期间锁、幂等和权限公开边界 | 作废追加反向流水；余额不伪造现金入账，采用 `DEC-FIN-020/024/027` |
| `01-整体架构与首页.md §4～§5`、`08-设置模块.md §3.1.3` | 全局列表/详情/表单规范；管理员和财务资金完整，销售主管资金只读；存在应收、账龄、收款、核销细权限 | 八项细权限与联系方式遮蔽采用 `DEC-FIN-028/029` |

本切片不实现供应商应付/付款、退款、转账、其他收支、机构收款或全局资金统计。退单/退款对已形成应收的冲销等待 `ORD-005/FIN-004`；在相应契约确认前，应收不得由页面编辑、删除或随订单重算。真实支付和真实文件上传不在原型范围。

### 页面与导航

| 页面 | 路由 | 本切片内容 |
|---|---|---|
| 应收列表 | `/finance/receivables` | 按客户汇总应收、已收、待收、最后收款和账期；查看明细/发起核销 |
| 应收明细（按单据） | `/finance/receivables/documents` | 订单级应收、订单/运费拆分、已收/待收和收款/核销入口 |
| 应收明细（按商品） | `/finance/receivables/products` | 使用订单保存的 SKU/名称/规格/数量/单价/小计快照；不实时回查价格 |
| 应收账龄 | `/finance/receivables/aging` | 按客户展示总未收和 `0～30天/31～60天/61～90天/91～180天/181～365天/365天以上` 账龄桶 |
| 收款单列表 | `/finance/receipts` | 收款单字段、核销状态、正常/已作废、查看/允许时作废/核销 |
| 新增收款单 | `/finance/receipts/new` | 普通客户收款；可由 `customerId/orderId` 预选，但 URL 不携带电话或金额等敏感快照 |
| 收款单详情 | `/finance/receipts/:receiptId` | 基本信息、资金流水、核销分配、附件元数据、状态和操作日志 |
| 核销单列表 | `/finance/writeoffs` | 本切片只显示收款核销；付款核销等待 `FIN-002` |
| 新增核销单 | `/finance/writeoffs/new` | 同一客户的待核销收款/预收与应收选择和金额分配 |
| 核销单详情 | `/finance/writeoffs/:writeoffId` | 收款、应收、逐条分配、金额、操作人、状态和允许时取消 |
| 订单收款 Tab | `/orders/:orderId?tab=receipts` | 用 FIN-001 provider 展示该订单应收和收款/核销；允许角色可从订单预填新增收款 |

资金二级导航增加“应收应付”“核销管理”“收付款管理”组，但只开放本切片真实页面；付款、应付和退款入口明确标为对应后续切片，不提供可点击假动作。

### 筛选与列表

应收列表按原文只有关键字（客户名称/编码或订单号）和单据日期范围；日期作用于应收形成时间而非订单创建时间。按客户汇总时，订单号命中表示返回含匹配应收的客户汇总。默认 `createdAt desc,id desc`、30 条/页，可选 10/30/50/100；金额合计基于完整查询结果，不只当前页。应收导出使用相同查询和权限，生成本地 CSV，不上传外部服务。

应收客户汇总固定列：

| 列 | 数据来源与规则 | 已确认决策 |
|---|---|---|
| 客户名称 | 应收形成时客户 ID、编码和名称快照；链接按单据明细并带 customerId | canonical 快照见 `DEC-FIN-017` |
| 应收金额 | 该客户查询范围内有效应收 `amountCents` 合计 | 退货调整等待 FIN-004 |
| 已收金额 | 有效核销的 `cashCents+discountCents`；现金可来自收款或预收 | 不等同收款单总额；`DEC-FIN-021/023` |
| 待收金额 | `应收金额-有效核销金额`，整数分且不得为负 | 超额整体拒绝 |
| 最后收款日期 | 该客户最后一张有效核销单的核销日期；没有则显示 `—` | `DEC-FIN-004/022` |
| 账期天数 | 最老未结应收从形成日第 0 天起的本地自然日年龄；没有未结应收显示 `0` | `DEC-FIN-004` |
| 操作 | 查看明细/核销 | 由 `DEC-FIN-028` 和是否存在可核销余额决定 |

按单据明细的关键字/日期与应收列表一致，固定列为单据日期、单据号、往来单位、联系人、联系电话、订货金额、运费、应收金额、已收金额、待收金额和操作。`订货金额 = 订单商品结算小计及优惠后的金额但不含运费`，`应收金额 = 订单保存的 orderAmountCents`，因此 `订货金额 + 运费 = 应收金额`；若历史快照不满足，显示数据错误而非前端补差。联系人/电话只取形成时快照，权限见 `DEC-FIN-029`。

按商品明细固定列为单据日期、单据号、客户、SKU 编码、商品名称、规格、数量、单价、金额和应收状态。数量携带订单单位快照；金额和单价为订单保存的整数分事实，不回查现价。每行继承单据的待收/部分收款/已收状态，不自行按 SKU 金额比例分摊（`DEC-FIN-025`）。

收款单和核销单原文没有筛选条件，本切片不添加行业惯例筛选。收款单固定列为收款单号、收款日期、客户名称、收款金额、收款方式、收款账户、核销状态、操作人、状态和操作；核销单固定列为核销单号、核销类型、核销日期、往来单位、核销金额、操作人、状态和操作。列表状态必须由领域结果给出，页面不能用金额临时推导后写回。

### 表单与校验

新增收款单：

| 字段 | 类型/必填 | 已确认规则 | 决策依据 |
|---|---|---|---|
| 客户 | 搜索选择/必填 | 选项来自 Customer public provider；保存快照；由订单进入时锁定为订单客户；允许收已停用/冻结客户历史欠款 | `DEC-FIN-019` |
| 关联订单 | ID/null/选填 | 由订单入口预选已形成且未结清应收；普通收款可空 | 多应收分配见 `DEC-FIN-022` |
| 收款日期 | DateTime/必填 | 上海受控时钟、不能晚于当前；月份开放且不得早于账户最新流水 | `DEC-FIN-027` |
| 收款金额 | integer cents/必填 | 正整数分，不能超过安全整数；未分配或超收部分形成可用预收 | `DEC-FIN-021` |
| 付款方式 | enum/必填 | 只采用现金/银行转账/微信/支付宝/余额；链支付明确 unavailable | `DEC-FIN-020` |
| 收款账户 | FundAccount ID/条件必填 | 现金/银行/微信/支付宝必须选择启用同类账户，银行另需启用资料；余额不选账户 | `DEC-FIN-020` |
| 是否立即核销 | boolean/否 | 默认否；开启时必须有至少一条同客户应收分配并与收款原子提交 | `DEC-FIN-022` |
| 核销分配 | allocation[]/条件必填 | 手工多对多；每项关联同一客户有效来源/应收，正整数分，不超过两侧可用额 | `DEC-FIN-021/022` |
| 优惠金额 | integer cents/否 | 只来自订单收款弹窗；与现金合计冲应收但不移动资金账户，正数时备注必填；自动计算 unavailable | `DEC-FIN-023` |
| 附件 | fake file metadata/null/否 | 最多一个 pdf/jpg/png ≤5MB；只保存虚构元数据，不上传真实文件或保存本地路径 | `DEC-FIN-030` |
| 备注 | string/null/否 | 去首尾空格；原文无长度，不臆造上限 | — |

新增核销单固定字段为客户、核销日期、待核销收款/预收来源、待核销应收、核销金额/分配和备注。收款与应收必须属于同一企业、同一客户、正常且有可用余额；单条和总额均不得超过任一侧可用余额或应收待收。保存应原子提交全部手工多对多分配，失败不留下部分核销；预收与优惠采用 `DEC-FIN-021～023`。

订单收款入口额外只读显示付款方、订单号、订单金额、已结算金额和订单未收；这些值由 Finance provider 返回，不能从订单页面临时累计。原文“本次收款金额”只是表单汇总，不保存第二份金额。线上链支付无真实接口，本切片显示“原型未接入”，不得生成成功收款。

### 持久化实体与公开边界

- `CustomerReceivable`：企业、业务编号、来源类型/ID/单号、客户/联系人快照、订单结算与商品行快照、应收/运费整数分、形成时间、结算方式/账期快照、requestId、版本和审计；不保存可被页面覆盖的已收/待收。
- `CustomerReceipt`：企业、业务编号、客户快照、日期、金额、方式、账户/资金流水引用、订单可选引用、优惠、附件元数据、备注、正常/作废审计、requestId 和版本。
- `ReceiptWriteoff`：企业、业务编号、客户、核销日期、状态、总额、备注、操作人、requestId、版本及 `WriteoffAllocation[]`；分配行明确来源收款/预收、目标应收、现金核销/优惠核销金额。
- `CustomerPrepaymentLedger`：客户预收增加、订单占用、释放和核销消费的不可变记录；可用、占用与已用由流水计算，不允许页面直接改余额（`DEC-FIN-021`）。
- 收款采用 `normal/void`、核销采用 `active/cancelled`，应收状态从有效核销派生为 `open/partial/settled`（`DEC-FIN-018/019/024/025`）。
- Finance 是应收、收款、核销和预收余额的唯一 owner；Order 只通过 `public.ts` 创建应收和读取订单收款投影，Customer 只读取应收余额 provider（`DEC-FIN-017`）。
- 收款确认必须通过 FIN-003 `public.ts` 追加账户收入；作废若允许，只能通过公开反向 command 追加可追溯流水，禁止直接改 `balanceAfterCents` 或删除原流水。

### 详情与操作矩阵

| 操作 | 入口与前置 | 成功结果 | 失败/恢复 | 决策门 |
|---|---|---|---|---|
| 发货形成应收 | ORD-004 发货；订单尚无应收 | 以同一 requestId 原子形成一次完整订单应收并完成发货 | Finance unavailable 或任一写入失败时发货/应收均不落地 | owner/集成 `DEC-FIN-017`；金额时点已 decided |
| 查看应收 | 四类应收页面；有查看权限 | 金额、来源和快照可追溯；provider 失败显式 error | 不把未知值显示 0 | `DEC-FIN-004/025/028/029` |
| 新增收款 | 客户、日期、金额、方式有效 | 按最终状态机原子写收款和必要资金流水；requestId 幂等 | 账户停用、期间锁、冲突或 provider 失败整体回滚并保留表单 | `DEC-FIN-019～023/027` |
| 立即核销 | 新增收款时选择有效应收 | 收款、流水、核销分配和预收变化单次提交 | 任一分配超额则全部失败 | `DEC-FIN-021～023` |
| 独立核销 | 同客户有效来源和应收 | 原子新增核销单并更新派生可用额/待收额 | 不留下半张核销单 | `DEC-FIN-021/022` |
| 作废收款 | 收款正常且满足最终条件 | 保留原单，写作废审计及必要反向资金流水 | 已核销/已结转/冲突时保持现场 | `DEC-FIN-019/024/027` |
| 取消核销 | 核销有效且满足最终条件 | 保留原单，恢复应收与收款/预收来源可用额，不重复动现金 | 级联或期间条件不满足时拒绝 | `DEC-FIN-024` |
| 订单查看/收款 | 订单已有应收且角色允许 | Tab 使用 Finance provider；新增页只传无敏感 ID | provider error 显示不可用，订单其他 Tab 仍可用 | `DEC-FIN-028/029` |
| 导出应收 | 查询合法且有导出/查看权限 | 本地 CSV 与完整查询合计一致 | provider error 不生成空文件冒充成功 | `DEC-FIN-028` |

### 状态与计算

已确认且不依赖开放决策：

```text
应收待收 = 应收金额 - 有效核销金额
收款可核销 = 正常收款金额 - 已分配现金核销金额
每项金额均为非负安全整数分
有效核销总额 <= 来源可用额 且 <= 目标应收待收
```

- 应收的已收/待收只由有效核销分配计算；收款单存在不代表应收已经结清。
- 一个 requestId 只能对应一种 command；重放返回原结果，复用于不同动作则 `CONFLICT`。可变聚合使用版本或 `expectedUpdatedAt` 防并发超额核销。
- 收款资金流水的发生时间与收款确认时间一致，账户余额由 FIN-003 计算；核销本身不再次改变资金账户。
- 任何错误不得留下“有收款无流水”“有核销无分配”“发货成功但应收失败”或预收一边更新的现场。
- 账龄按上海自然日计算；形成日为第 0 天。预收、优惠、作废、取消和日期约束严格采用 `DEC-FIN-004/021/023/024/027`，不得由页面另行推导或放宽。

### 权限与敏感信息

细权限固定为 `finance.view-receivables`、`finance.view-receivable-details`、`finance.view-aging`、`finance.view-receipts`、`finance.manage-receipts`、`finance.view-writeoffs`、`finance.manage-writeoffs`、`finance.export-receivables`。管理员/财务全量，销售主管查询与导出只读，业务员/仓库无权；路由、UI、Store 和 Service 必须一致（`DEC-FIN-028`）。

联系人、电话、客户银行资料、附件和资金账户均按敏感信息处理。列表 URL、日志、错误和导出文件名不得含电话、账号或附件路径；管理员/财务看完整快照，销售主管电话遮蔽且导出一致（`DEC-FIN-029`）。附件只保留虚构元数据，不读取或上传真实文件内容。

### 页面状态与 Mock

最终 baseline 至少需要：两个客户、三张不同形成日的订单应收、未收/部分/已结清结果、一张普通未核销收款、一张部分核销收款、有效/已取消核销、预收占用（若确认）、多个资金账户、当前开放月和已结转月。全部客户、电话、单号、附件和摘要明显虚构；应收必须引用真实 baseline 订单 ID，不能复制一套孤立订单。

| 场景 | 必备行为 | 页面期望 |
|---|---|---|
| normal | 应收、收款、核销、订单 Tab、账户流水可追溯 | 汇总与逐单金额守恒，确定动作可重复 |
| empty | 无应收/收款/核销，客户和账户可独立存在 | 各页成功空状态；不能核销；有写权限且入账条件满足时可新建未分配收款并形成预收 |
| error | 主 Repository 或跨领域写入稳定失败 | 表单/弹窗保留，重试可用，无半事务 |
| slow | 固定延迟 | loading 和防重复提交，完成后恢复 |
| permission-denied | 无资金权限角色或缺 command 权限 | 路由/UI/Store/Service 四层拒绝 |
| partial-failure | 应收主数据成功但订单/客户 lookup 局部失败，或资金账户 provider 失败 | 可用主体继续显示，缺失快照标记 error/unavailable；写动作关闭 |
| boundary | 0 待收、1 分、最大安全整数、同日/账龄分界、刚好全额/超 1 分 | 精确进入状态或明确拒绝，无浮点误差 |
| concurrent | 两会话同时核销同一应收、作废同一收款或发货形成应收 | 首个成功，后续 `CONFLICT`，刷新后金额一致 |

### 验收追踪

| 追踪项 | 必须证明 | 自动验证 | 人工路径 |
|---|---|---|---|
| `FIN001-A01` / FIN-01 | 一张收款可部分核销并可匹配多张应收，超额整体拒绝 | allocation/schema/service/property tests | 新建部分收款→多单核销→核对三处余额 |
| `FIN001-A02` / FIN-03 | 收款只更新一次正确账户，余额方式不伪造入账 | finance command/idempotency/cross-repo rollback | 收款前后账户明细与逐笔余额 |
| `FIN001-A03` / FIN-04 | 已核销收款不可改金额/删除；作废和取消按确认状态机追溯 | lifecycle/period/concurrency tests | 合法与非法作废/取消及错误恢复 |
| `FIN001-A04` / FIN-02 | 受控日期在所有账龄/到期边界得到确定结果 | clock/bucket/due-date tests | 切换边界场景核对账龄合计 |
| `FIN001-A05` / ORD 集成 | 发货一次形成 canonical 应收，签收不重复，失败全回滚 | order-finance integration tests | 重置后完成发货→应收→订单 Tab |
| `FIN001-A06` / 预收与优惠 | 占用、可用、消费、取消及优惠保持金额守恒 | prepayment/discount invariant tests | 预收订单核销和优惠收款 |
| `FIN001-A07` / 页面字段 | 原文各筛选、列、表单、详情、状态和操作可回指本契约 | UI contract tests | 六类页面、订单 Tab、empty/error/slow |
| `FIN001-A08` / 权限隐私 | 四层权限一致，电话/附件/账户不从 URL 或日志泄露 | route/store/service/masking tests | 五角色切换、导出与控制台检查 |
| `FIN001-A09` / 确定性 | reset 哈希一致，干净启动仍可重复黄金旅程 | `npm run verify`、`mock:reset` hash | 1280px、键盘、控制台、停止/启动复验 |

### 已确认方案（2026-08-14）

| 决策 | 已确认结论 |
|---|---|
| `DEC-FIN-004` | Asia/Shanghai 自然日；形成日第 0 天，首桶 `0～30天`。现结形成日到期、账期按快照天数、月结为形成月末；当前日期晚于到期日且有待收才逾期。客户汇总取最老未结年龄和最后有效核销日。 |
| `DEC-FIN-017/018` | Finance 是应收唯一 owner；订单发货通过 Finance public command 原子创建并只读投影。应收保存形成时快照，不可编辑/删除/随订单重算，未来更正走 `ORD-005/FIN-004` 调整单。 |
| `DEC-FIN-019/020` | 收款保存即确认且不可改金额/方式/账户；允许收停用/冻结客户历史欠款。现金/银行/微信/支付宝必须匹配启用同类账户，银行还需启用资料；余额只消费预收、不产生资金入账；链支付 unavailable。 |
| `DEC-FIN-021/022` | 未分配或超额到账形成可用预收；订单占用保持绑定，核销消费后可按取消结果恢复。核销为同客户手工多对多分配矩阵，不自动 FIFO；立即核销默认否且与收款原子提交。 |
| `DEC-FIN-023` | 手工优惠必须有备注；`cashCents+discountCents` 冲减应收，只有 cash 消耗收款/移动资金；不自动计算优惠。 |
| `DEC-FIN-024` | 只有当前开放自然月且无有效核销的收款可因 1～200 字原因作废，追加同账户反向流水。核销只能因必填原因整张取消，恢复应收和收款/预收来源，不再次移动现金；后续依赖或冲突时拒绝。 |
| `DEC-FIN-025/026` | 商品行继承单据待收/部分收款/已收三态，不做比例分摊。单号为企业日序列 `YS/SK/HX-YYMMDD-#####`，并发唯一且不复用。 |
| `DEC-FIN-027` | 日期使用上海受控时钟且非未来；收款月未结转且不早于账户最新流水，核销不早于所有来源；requestId 幂等，版本冲突刷新。 |
| `DEC-FIN-028/029` | 管理员/财务全部；销售主管拥有应收、账龄、收款/核销及导出只读，订单 Tab 按订单范围，业务员/仓库无 FIN-001 权限。管理员/财务看完整联系人电话，主管电话遮蔽；URL/日志/错误不记录完整电话。 |
| `DEC-FIN-030` | 凭证最多 1 个 pdf/jpg/png 且≤5MB；原型只保存虚构文件名、MIME、大小和 fake ID，不读取、上传或保存本地路径。 |

以上方案已由用户明确回复“FIN-001 全部按推荐方案执行”确认。`FIN-001` 已按完成计划通过完整自动与人工验收并标记 passing。

## FIN-002 字段与交互契约（ready）

当前状态：`ready`。用户于 2026-08-20 回复“FIN-002 全部按推荐方案执行”，`DEC-FIN-041～054` 已全部写回并决定。`FIN-002` 依赖 `FIN-003` 资金账户/期间公开 command 与 `PUR-002` 采购订单/入库事实；本节是供应商应付、付款与付款核销的实现契约。

本契约追踪 `07-资金模块.md §2.5～§2.7、§3、§4.3～§4.4`、`01-整体架构与首页.md §2、§4～§5` 以及 `FIN-01～FIN-04`；`FIN-03` 账户余额、`FIN-04` 已核销不可改和 `FIN-02` 账龄均通过本切片的公开命令和验收规则落地。

### 事实来源

| 能力 | 原始字段/行为 | 已知边界 | 决策门 |
|---|---|---|---|
| 应付列表 | 供应商、应付金额、已付金额、待付金额、最后付款日期、查看/付款 | `07-资金模块.md §2.5` 未说明形成时点、金额来源、作废/退单投影 | `DEC-FIN-041～043` |
| 应付按单据 | 单据日期、采购单/退单号、供应商、采购金额、应付、已付、待付、付款/核销 | `PUR-002` 只保存采购成交和入库快照，`PUR-003` 尚未实现 | `DEC-FIN-041～043、053` |
| 应付按商品 | 单据、供应商、SKU、规格、数量、采购单价、金额、应付状态 | 部分入库与商品行状态没有资金分配规则 | `DEC-FIN-042、043` |
| 供应商账龄 | 按单据日期计算账龄 | 应付起算日、区间端点、负数贷项未定义；客户账龄结论不可自动套用 | `DEC-FIN-044` |
| 付款列表/表单 | 付款单号、日期、供应商、金额、现金/银行转账、付款账户、立即核销、附件、备注、正常/已作废 | 是否保存即确认、是否财务审核、账户匹配和附件原型边界未定义 | `DEC-FIN-045～048` |
| 付款核销 | 供应商、核销日期、付款单、应付单、核销金额、备注；查看/取消 | 是否沿用 FIN-001 多对多分配、取消规则和供应商一致性未明确 | `DEC-FIN-049～050` |
| 采购交接 | `PUR-002` 订单/入库状态、供应商/商品/单位/价格/金额快照 | 应付形成时点与部分入库计价未定义；采购退单属于后续 `PUR-003` | `DEC-FIN-041～043、053` |

### 页面与导航

| 页面 | 路由 | 内容与边界 |
|---|---|---|
| 供应商应付列表 | `/finance/payables` | 按供应商汇总应付、已付、待付、最后付款日期；支持按单据/商品明细切换，不显示未形成的采购应付 |
| 应付详情 | `/finance/payables/:payableId` | 供应商/采购单快照、入库分录、贷项、付款和核销记录；退采未接入时显示 unavailable |
| 供应商账龄 | `/finance/payables/aging` | 按供应商展示六档账龄和逾期金额；采用受控 Asia/Shanghai 日期 |
| 付款单 | `/finance/payments` | 付款列表、创建、详情、受限作废；真实银行/支付通道不接入 |
| 付款核销 | `/finance/writeoffs` | 在既有核销工作区增加付款核销视图；收款核销和付款核销不混淆来源 |

`/finance` 入口仍由 `FIN-003` 资金工作区管理；本切片不开放通用退款、转账、其他收支和机构统计页面。

### 筛选与列表

- 普通列表沿用全局默认 30 条/页、创建时间倒序和稳定 `createdAt desc,id desc` 排序；筛选写入 URL，过期查询响应丢弃。
- 应付列表支持供应商/采购单号关键字、应付形成日期范围、应付状态和付款状态；按单据和按商品使用同一份一次性 provider snapshot，汇总不受分页影响。
- 付款列表支持付款单号/供应商关键字、付款日期范围、付款方式、核销状态和 `normal/voided` 状态；已核销金额、待核销金额由不可变分配计算。
- 导出仅管理员/财务允许，导出当前筛选全量 UTF-8 CSV；销售主管只读导出且供应商电话按快照遮蔽，不写入 URL、日志或错误。

### 表单与校验

- 新增付款必填供应商、付款日期、整数分金额、付款方式和付款账户；现金匹配启用 cash 账户，银行转账匹配启用 bank 账户与启用银行资料。
- 付款日期使用 Asia/Shanghai 受控时钟，禁止未来日期和已结转月份；金额必须为正整数分且不能导致账户负余额；停用账户不能承接新付款。
- 立即核销默认关闭；开启后选择同供应商付款单/应付单并填写多对多分配，分配合计不得超过付款可用额或应付待付额。
- 附件最多一个 pdf/jpg/png 且不超过 5MB，原型只保存虚构文件元数据；备注沿用全局长度校验，不保存真实路径。
- 创建、作废、核销命令携带 `requestId` 和 `expectedVersion`；重复请求返回原结果，版本冲突保留输入并要求重新加载。

### 详情与操作矩阵

| 操作 | 管理员/财务 | 销售主管 | 业务员/仓库 | 关键前置 |
|---|---|---|---|---|
| 查看应付/账龄 | 允许 | 允许，只读且电话遮蔽 | 拒绝 | provider available；失败显示 error/unavailable |
| 新增付款 | 允许 | 拒绝 | 拒绝 | 供应商、账户、期间、余额和金额校验 |
| 作废付款 | 允许 | 拒绝 | 拒绝 | 当前开放月、无有效核销、填写原因，追加反向流水 |
| 创建/取消付款核销 | 允许 | 拒绝 | 拒绝 | 同供应商、开放月、版本和分配守恒 |
| 导出 | 允许 | 允许，只读脱敏 | 拒绝 | 当前筛选全量，敏感字段按角色处理 |

Finance 是应付、付款、核销和供应商应付贷项唯一 owner；采购和库存只通过公开 provider 交接，不直接改资金 repository。

### 状态与计算

- 有效入库按行累计形成应付；行应付为入库采购单位数量×成交单价换算金额，赠品为 0；订单级优惠/其他费用按原价比例分摊，末次分配吸收分尾差。
- `待付 = 应付 - 有效核销金额 - 有效贷项金额`，不得小于 0；`已付`为有效付款被核销的金额，不把未核销付款误计为已付。
- 应付状态：无有效应付不显示；`unpaid`、`partially-paid`、`paid`、`voided`。付款状态：`normal/voided`；核销状态：`confirmed/cancelled`。
- 账龄六档为 `0～30/31～60/61～90/91～180/181～365/365+`；形成日为第 0 天，供应商账期在形成时快照，超过到期日且待付大于 0 才逾期。
- 付款确认、账户流水、核销分配和应付投影在一个原子边界内；失败不留下半付款、半流水、半核销或错误余额。

### 页面状态与 Mock

| 场景 | 可见语义 | 数据要求 |
|---|---|---|
| `normal` | 应付、付款、核销和账龄可用 | 由采购入库和资金账户确定性事实产生 |
| `empty` | 各列表空状态 | 不以错误或 0 伪装无数据 |
| `error` | 可理解错误和重试 | 状态哈希不变，不留半事务 |
| `slow` | 提交中禁重复，旧响应不覆盖新版本 | requestId 最多生效一次 |
| `permission-denied` | 路由拒绝且不显示写入口 | Service 直接调用同样拒绝 |
| `partial-failure` | 单一 provider 失败不抹掉其他可用视图 | 付款/账户/核销跨域失败整体回滚 |
| `unavailable` | 退采、发票、真实支付明确未接入 | 不显示 0 或已付款 |
| `concurrent` | 首个版本成功，后续 `CONFLICT` | 要求重新加载 |
| `boundary` | 日期、金额、余额、锁月、超额、附件失败可复现 | reset 前后 baseline 哈希一致 |

### 验收追踪

| 验收 ID | 来源/决策 | 可观察结果 | 自动证据 | 人工证据 |
|---|---|---|---|---|
| `FIN002-A01` | §2.5～§2.7、`DEC-FIN-041～044` | 入库形成应付、部分金额与账龄正确 | schema/service/aging tests | 应付列表、明细、账龄边界 |
| `FIN002-A02` | §4.3～§4.4、`DEC-FIN-045～048` | 付款只扣一次正确账户，作废受限 | payment/account/idempotency tests | 创建、详情、作废和锁月 |
| `FIN002-A03` | §3、`DEC-FIN-049～050` | 手工多对多核销、部分/全部和取消守恒 | allocation/concurrency tests | 核销表单、超额/跨供应商拒绝 |
| `FIN002-A04` | §5、`FIN-03/04/06`、`DEC-FIN-051～052` | 余额、流水、权限和敏感字段一致 | permission/atomicity tests | 五角色和电话遮蔽 |
| `FIN002-A05` | `DEC-FIN-053～054`、`RELIABILITY.md` | 退采 unavailable、九场景稳定、干净重启可复现 | mock/reset/partial-failure tests | 1280px 黄金旅程和控制台无错误 |

### 已确认方案

- 每次有效采购入库按实际入库数量形成不可变应付分录；审核但未入库不形成，全部入库后完整。入库行按数量×成交单价换算，赠品为 0，订单优惠/其他费用按原价比例分摊，末次承接分尾差。
- 应付状态为 `unpaid/partially-paid/paid/voided`；未入库不建应付，已有应付不因采购作废删除。供应商账龄使用上海自然日和六档账龄，形成日为第 0 天，账期在形成时快照，贷项先抵待付。
- 业务付款保存即确认并原子扣账，不设审核态；现金匹配启用 cash，银行转账匹配启用 bank 和银行资料；停用账户、锁定期间、余额不足整体拒绝。付款号为企业内 `FK-YYMMDD-#####` 日序列且不复用。
- 立即核销默认关闭，付款可后续核销；显式立即核销时与多对多分配共用原子命令。核销沿用手工多对多矩阵，供应商必须一致，支持部分/全部核销，不自动 FIFO，核销号复用 `HX-YYMMDD-#####`。
- Finance 是付款、账户流水、核销和应付余额唯一 owner；requestId 幂等、expectedVersion 冲突拒绝，跨域失败恢复快照。管理员/财务全量读写导出，销售主管只读且电话遮蔽，业务员/仓库拒绝。
- 提供不可变供应商应付贷项 command：先冲待付，超出部分形成待处理供应商退款义务；退单主体由后续 `PUR-003` owner 实现。未接入退单、发票、支付渠道和真实银行数据显示 unavailable，不以 0 或“已付款”替代。

### 状态、权限与场景候选

应付状态为 `unpaid/partially-paid/paid/voided`，付款状态为 `normal/voided`，核销状态为 `confirmed/cancelled`；路由、UI、Runtime、Service 四层一致。Mock 必须支持 `normal/empty/error/slow/permission-denied/partial-failure/unavailable/concurrent/boundary`，未知资金结果禁止展示为 0。

### 验收细节

| 验收 ID | 目标 | 通过条件 |
|---|---|---|
| `FIN002-A01` | 应付来源和金额 | 采购入库按已确认规则形成应付快照；部分/多次入库与总额守恒；未接入退单不伪造冲减 |
| `FIN002-A02` | 付款与账户 | 付款确认最多一次写入指定启用账户；账户余额、流水和付款状态原子一致；余额不足/锁定月整体拒绝 |
| `FIN002-A03` | 付款核销 | 同供应商多对多手工分配，支持部分/全部核销；超额、跨供应商和重复 requestId 被拒绝或幂等返回 |
| `FIN002-A04` | 账龄与权限 | 受控 Asia/Shanghai 日期边界正确；五角色路由/UI/Service 权限一致，敏感字段按契约遮蔽 |
| `FIN002-A05` | 可靠性 | error/partial-failure/concurrent 不留下半应付、半付款、半流水或错误余额；reset 与干净重启可复现 |

## ORD-005 退单退款集成契约（ready）

`ORD-005` 需要 Finance 提供退货应收贷项、退款义务、确认退款和订单/退单只读投影，但不允许 Order 直接修改 `CustomerReceivable`、核销、预收账、账户流水或期间。以下是原文已知边界：

- 确认退款列表显示退款单号、原订单号、客户、退单/售后类型、申请金额、实退金额、原路/余额/现金方式、申请时间、待审核/已退款/已拒绝和审核/退款/查看。
- 退款优先原支付方式，特殊情况可现金或余额；资金账户实时更新，已结转月份禁止资金出入，账户仍禁止负余额。
- 原应收、收款、核销和资金流水不可编辑或删除；退货必须追加可追溯调整，不能把原应收金额改小或把历史核销删掉。
- 普通客户退货遵循审核→退货入库→确认退款；短装差异退款没有入库事实，必须使用独立来源类型。

用户于 2026-08-14 确认全部推荐方案：最小对象为 `ReceivableCreditAdjustment`、`CustomerRefund`、`RefundSourceAllocation` 和 `RefundRequestRecord`。贷项保存退单/差异来源、原应收、金额、发生时间与操作者；退款保存申请/实退、方式拆分、账户/预收来源、状态和审计。Finance 是上述对象唯一 owner；贷项先减少未收应收，超出部分才形成退款义务；原路按有效核销来源时间/id 升序追溯现金与预收，优惠不可退款；余额退款恢复可用预收且不写资金流水；财务一次确认原子完成，已退款不可撤销，拒绝后按剩余额度新建申请。ready 复查补登并按全部推荐授权确认 `DEC-FIN-037～039`：退款编号采用企业日序列 `TK-YYMMDD-#####` 且不复用；完整入库形成贷项后按全部剩余义务自动创建待审核申请，确认必须整笔完成；原路账户不可改，特殊现金需选择启用 cash 账户并填原因，余额不选账户，全部分配原子提交。`ORD-005` 只开放 `customer-return` 所需窄契约，不宣称通用 `FIN-004` 完成。

安全的不变量：累计有效贷项不超过原应收；调整后待收不小于 0；累计实退不超过可追溯真实现金或预收消费；优惠不形成现金退款；同一来源同一时点不重复形成有效贷项；任一账户/期间/版本/跨域写失败时 Order、Inventory、Finance 全部恢复。按 `DEC-FIN-040`，退款前作废入库时原贷项不删除而标记已冲销，待退款关闭；重新入库创建新贷项和新退款号，已退款后禁止作废。仅在上述窄契约的代码和证据完成后开放退单退款入口；转账、其他收支与通用退款更正仍保持后续切片禁用状态。

## FIN-004 字段与交互契约（ready）

当前状态：`ready`。用户于 2026-08-24 确认“FIN-004 + FIN-005 全部按推荐方案执行”，`DEC-FIN-055～063` 已逐项写回。客户退款继续复用唯一 `CustomerRefund`；供应商退款到账、转账与其他收支采用独立不可变对象；拉新提现因来源 provider 缺失保持明确 unavailable。本节现在是可编码契约。

已确认方案：供应商退款义务整笔到账，只能进入启用的 cash/bank 账户且不得超额；转账创建后待审核，批准时原子生成同额双流水，取消和反向纠错保留完整历史；其他收支待审核，批准才入账，已批准错误通过关联的反方向单据更正；收支项目由 Finance 按方向管理，同方向名称唯一，历史引用后不可删除。三类单号分别采用 `ZZ/QTSK/QTFK-YYMMDD-#####`，业务日期不得未来、必须处于开放期间且不早于相关账户最近流水，所有命令使用 requestId 与版本防重。超级管理员/财务可管理，销售主管只读并遮蔽敏感信息，业务员/仓库拒绝。

### 事实来源

| 来源 | 可直接采用的事实 | 缺失事实/决策门 |
|---|---|---|
| `07-资金模块.md §4.5`、FIN-05 | 确认退款十列；退款优先原路，特殊可现金/余额；状态待审核/已退款/已拒绝 | 客户/供应商对象是否合并、完成后更正 `DEC-FIN-055～057` |
| `07-资金模块.md §4.6`、FIN-10 | 拉新提现八列；提现需审核后打款 | 奖励账本、申请人与余额 provider 不存在 `DEC-FIN-062` |
| `07-资金模块.md §5.4～§5.5` | 其他收付款明细八列；按收支项目汇总收入、支出和净额 | 与 §7 单据关系、审核后入账和历史更正 `DEC-FIN-059～061` |
| `07-资金模块.md §6`、FIN-08 | 转账列表八列和六个表单字段；只允许内部账户调拨 | 审核、取消、双流水与完成后纠错 `DEC-FIN-058/061` |
| `07-资金模块.md §7`、FIN-07 | 其他收款/付款列表各九列和七个表单字段；非业务收支需审核 | 项目 owner、状态终态、入账和更正 `DEC-FIN-059～061` |
| `FIN-001/003` passing | CustomerRefund、预收账、账户、不可变流水、期间锁、余额非负和附件 fake 边界已经存在 | 不允许复制退款或直接改余额；复用见 `DEC-FIN-055/056` |
| `FIN-002/PUR-003` passing | 退采贷项先冲待付，超出形成供应商退款义务；采购投影等待实际到账 | 到账方式、账户和完成条件 `DEC-FIN-057` |
| `01-整体架构与首页.md §4～§5` | 列表、表单、详情、稳定分页和五角色的全局交互规则 | 细粒度权限采用 `DEC-FIN-063` |
| 全局 §4～§5、设置 §3.1.3 六 | 默认排序/分页、详情/表单通则和查看/管理细权限名 | 五角色默认映射 `DEC-FIN-063` |

### 页面与导航

路由是工程组织，不新增业务字段；资金工作区继续使用一个共享 Runtime，新增页面不得复制“原型控制”条。

| 页面 | 推荐路由 | 原文内容与边界 |
|---|---|---|
| 确认退款 | `/finance/refunds` | 复用现有客户退款列表/详情；是否增加供应商退款到账 Tab 由 `DEC-FIN-055/057` 决定 |
| 拉新提现 | `/finance/withdrawals` | 保留原文入口和八列；provider 缺失时按 `DEC-FIN-062` 明确 unavailable |
| 转账管理 | `/finance/transfers`、`/finance/transfers/new`、`/finance/transfers/:id` | 列表、新增、详情、审核/取消；不接银行外部划款 |
| 其他收付款明细/汇总 | `/finance/other-transactions` | 明细与按项目汇总两个 Tab；只消费其他收支单据，不复制账户流水事实 |
| 其他收款 | `/finance/other-receipts`、`/finance/other-receipts/new`、`/finance/other-receipts/:id` | 非销售业务收入的列表、创建、详情和审核 |
| 其他付款 | `/finance/other-payments`、`/finance/other-payments/new`、`/finance/other-payments/:id` | 非采购业务支出的列表、创建、详情和审核 |

“新增任意客户退款”“手工修改既有退款金额”“直接改账户余额”“真实提现/转账/打款”均不是本批次页面能力。供应商退款若确认独立对象，只能从已存在的供应商退款义务发起。

### 筛选与列表

- 全部普通列表沿用 `createdAt desc,id desc` 稳定排序、默认 30 条/页和 10/30/50/100；筛选进入 URL，合计基于完整筛选结果，不只当前页。原文没有导出要求，本切片不增加 CSV/Excel。
- 确认退款固定列：退款单号、原订单号、客户名称、退款类型、申请退款金额、实退金额、退款方式、申请时间、状态、操作。客户退款继续从 `CustomerRefund` 与 `ReceivableCreditAdjustment` 读取；退款类型只显示已有 customer-return/short-shipment 映射，不从文案猜“售后退款”。
- 拉新提现固定列：提现人、提现人编码、提现人类型、联系方式、提现金额、提现状态、申请时间、操作；未知 provider 不能用空数组、0 或虚构姓名冒充业务数据。
- 转账固定列：转账单号、转账日期、转出账户、转入账户、转账金额、转账状态、操作人、操作。待审核不进入账户余额；完成结果必须能链接两条同源资金流水。
- 其他收款/付款列表固定列：单号、日期、往来单位、收入/支出项目、金额、入/出账账户、经办人、状态、操作。
- 其他收付款明细固定列：日期、单据号、单据类型、往来单位、金额、收支项目、经办人、状态；汇总固定列：收支项目、收入金额、支出金额、净额。只统计最终有效单据，具体状态由 `DEC-FIN-059` 决定。
- 原文没有给上述列表筛选项；在产品确认前只保留页面所需状态 Tab/类型切换和全局分页，不自行增加关键字、日期、账户等业务筛选。

### 表单与校验

转账表单严格包含转出账户、转入账户、转账金额、转账日期、附件、备注。账户来自 FIN-003 启用账户；同一账户不能同时转出/转入；金额为正安全整数分，不允许手续费、汇率或跨币种；日期、期间、余额、审核入账和附件采用 `DEC-FIN-058/061` 的最终结论。

其他收款表单严格包含付款方、收款日期、收入项目、收款金额、入账账户、附件、备注；其他付款对应收款方、付款日期、支出项目、付款金额、出账账户、附件、备注。项目的新增/停用和往来单位快照由 `DEC-FIN-060` 决定；金额正整数分，账户必须启用，支出不能造成负余额。

客户退款没有手工新增表单，只能由有效贷项形成。待审核退款继续使用既有确认/拒绝/重提规则；已退款更正和供应商退款到账分别受 `DEC-FIN-056/057` 门控。所有附件仅保存虚构文件名、MIME、大小和 fake ID，不读取真实文件内容或本机路径。

### 详情与操作矩阵

| 操作 | 前置 | 成功结果 | 失败/恢复 | 决策门 |
|---|---|---|---|---|
| 查看/确认客户退款 | 已有退款、角色允许 | 复用现有退款详情、原路/现金/余额分配和审计 | 账户、期间、余额、版本任一失败整体回滚 | 已确认规则 + `DEC-FIN-055/056` |
| 确认供应商退款到账 | 有有效供应商退款义务 | 追加收入流水并完成义务投影 | 不超义务、不重复到账、不留半流水 | `DEC-FIN-057` |
| 新建转账 | 两个有效内部账户、金额/日期合法 | 创建待审核转账，不提前动账 | 表单保留；requestId 重放不重复编号 | `DEC-FIN-058/061` |
| 审核转账 | 待审核且版本匹配 | 同事务写转出和转入，同额、同源、总资金不变 | 任一账户失败则转账和两流水全回滚 | `DEC-FIN-058` |
| 取消/纠正转账 | 满足最终状态前置 | 待审核取消或新反向关联单 | 不删除已完成转账/流水 | `DEC-FIN-058` |
| 新建其他收支 | 表单合法、角色允许 | 保存待审核单，不动账 | 失败不占编号或产生流水 | `DEC-FIN-059～061` |
| 审核其他收支 | 待审核、账户/期间/余额有效 | 原子批准并写一条 income/expense 流水 | 拒绝/冲突保持账户不变 | `DEC-FIN-059` |
| 审核/打款拉新提现 | 奖励与申请 provider 可用 | 按未来契约执行 | 当前明确 unavailable，不产生流水 | `DEC-FIN-062` |

### 状态与计算

已确认的硬不变量：金额全部使用整数分；账户余额只由 `FundMovement` 和期初计算；已结转月份禁止资金出入；支出后余额不得为负；一个 requestId 只能对应一种 command；页面不得直接改投影金额。

已确认公式与状态：

```text
转账：pending-review -> completed | cancelled
完成转账：transfer-out.amountCents = transfer-in.amountCents = transfer.amountCents
企业总资金变化：-transfer-out + transfer-in = 0

其他收支：pending-review -> approved | rejected
批准其他收款：一条 other-income
批准其他付款：一条 other-expense
项目净额 = approved income - approved expense
```

客户退款继续使用 `pending/refunded/rejected`，退款完成后原对象和原流水不可删除。供应商退款义务、转账和其他收支需要独立不可变对象、操作人快照、版本、requestId、源单号与审计日志；不得用一条可编辑“账户余额调整”同时冒充多个业务类型。

### 页面状态与 Mock

| 场景 | 本批次要求 |
|---|---|
| `normal` | 可从零创建待审核转账和其他收支，审核后账户/流水/汇总守恒；客户退款继续可追溯 |
| `empty` | 各列表显示无数据并保留合法新增入口；提现/机构依赖缺失不是 empty |
| `error` | 查询可重试，写入失败不留半单/半流水 |
| `slow` | 复用共享加载/提交状态，按钮禁重复，旧响应不覆盖新状态 |
| `permission-denied` | 路由/UI/Store/Service 一致拒绝 |
| `partial-failure` | 退款/供应商义务 provider 失败不抹掉独立的转账/其他收支查询；跨对象写仍整单回滚 |
| `unavailable` | 拉新奖励/人员或其他真实未接 provider 明确不可用，不显示 0 |
| `concurrent` | 同一待审核单首个版本成功，后续 `CONFLICT` |
| `boundary` | 0/负数/超安全整数、同账户转账、余额不足、锁月、未来/倒序日期、附件失败可复现 |

### 验收追踪

| 验收 ID | 来源/决策 | 可观察结果 | 自动证据 | 人工证据 |
|---|---|---|---|---|
| `FIN004-A01` | §4.5、FIN-05、`DEC-FIN-055～057` | 客户退款复用唯一事实，供应商义务/更正不重复或越界 | owner/refund-limit/idempotency tests | 退款列表、详情和来源追溯 |
| `FIN004-A02` | §6、FIN-08、`DEC-FIN-058/061` | 审核前不动账，审核后双流水同额且企业总额不变 | transfer atomicity/period/concurrency tests | 新建→审核→反向纠错旅程 |
| `FIN004-A03` | §5.4～§5.5、§7、FIN-07、`DEC-FIN-059～061` | 其他收支审核入账、项目汇总和更正守恒 | other-income/expense/summary tests | 收款与付款各一条代表性任务 |
| `FIN004-A04` | §4.6、FIN-10、`DEC-FIN-062` | 缺少奖励账本时提现明确 unavailable 且零副作用 | unavailable/no-movement tests | 页面边界说明 |
| `FIN004-A05` | 设置权限、`DEC-FIN-063` | 五角色四层权限和敏感字段一致 | permission/bypass tests | 代表性角色切换 |
| `FIN004-A06` | L3 可靠性 | 幂等、并发、失败回滚、金额与账户守恒 | service/integration/full verify | 从 reset 开始的 1280px 黄金旅程 |

## FIN-005 字段与交互契约（ready）

当前状态：`ready`。用户于 2026-08-24 确认“FIN-004 + FIN-005 全部按推荐方案执行”，`DEC-FIN-064～070` 已逐项写回。本切片复用 canonical 收款、应收/贷项/核销、账户流水和期间投影，不保存第二套统计事实；机构 provider 未接前只使机构页 unavailable，不影响其余统计。

已确认方案：全部报表统一使用 Asia/Shanghai 自然月，默认 Mock 当前月，未来月拒绝且 `month` 写入 URL；机构汇总只纳入带机构快照的 normal 实际客户收款，余额和作废不进入合计；现金方式统计只纳入 cash/wechat/alipay/bank 的 normal 客户实收。订单支付按一个订单应收一行计算净应收、净已收与待收，并保存真实 `orderedAt`；账户统计完全复用 `FundMovement` 与 FIN-003 期间投影。四张统计使用 `/finance/statistics` 单路由四 Tab、30 条稳定分页；超级管理员/财务/销售主管可看，仅超级管理员/财务可导出当前筛选全量 CSV。

### 事实来源

| 来源 | 可直接采用的事实 | 缺失事实/决策门 |
|---|---|---|
| `07-资金模块.md §8`、FIN-11 | 机构列表六列、明细七列，多机构按机构汇总收款 | 机构 provider、快照、收款状态/方式 `DEC-FIN-064/065` |
| `07-资金模块.md §9.1` | 按现金/微信/支付宝/银行转账显示金额、笔数、占比 | 预收/余额/作废/其他收支范围和 0 分母 `DEC-FIN-067` |
| `07-资金模块.md §9.2` | 三张应收/已收/待收卡片和十列订单支付明细 | 退货贷项、净额、下单时间和行粒度 `DEC-FIN-068` |
| `07-资金模块.md §9.3～§9.4` | 账户交易七列和账户汇总六列 | 与 FIN-003 重复、movement 范围和转账双边 `DEC-FIN-069` |
| `FIN-001/002/003/004` | canonical 应收/应付、收付款/核销/退款、账户不可变流水和其他收支事实 | 查询必须复用同一 Repository 快照，不从页面累计 |
| `01-整体架构与首页.md §4～§5` | 列表、详情、稳定分页和五角色的全局交互规则 | 统计导出与敏感字段采用 `DEC-FIN-070` |
| 全局 §4～§5、设置 §3.1.3 六 | 30 条分页、列表/详情规则和查看/导出资金统计权限名 | 时间、导出、五角色与敏感字段 `DEC-FIN-066/070` |

### 页面与导航

| 页面 | 推荐路由 | 内容与边界 |
|---|---|---|
| 机构收款列表 | `/finance/institution-receipts` | 机构累计/本期/笔数/最后时间；机构 provider 未接时整页 unavailable |
| 机构收款明细 | `/finance/institution-receipts/:institutionId` | 使用收款形成时机构快照，不按当前组织关系改写历史 |
| 资金统计 | `/finance/statistics` | 单路由四 Tab：收款现金、订单支付、账户交易明细、账户汇总；`report` 与期间写 URL |

账户汇总必须复用 FIN-003 期间投影；不得新建第二套余额表。资金统计是只读查询，不自动生成核销、结转、退款、转账或其他收支。

### 筛选与列表

- 机构列表固定列：机构名称、收款总额、本期收款、收款笔数、最后收款时间、操作。机构详情固定列：机构名称、收款日期、客户名称、收款金额、收款方式、收款状态、收款单号。
- 收款现金统计固定列：收款方式、收款金额、收款笔数、占比；四种方式即 cash/wechat/alipay/bank 的业务标签，不增加“其他”桶掩盖未知数据。
- 订单支付统计顶部固定显示应收金额总计、已收金额总计、待收金额总计；列表固定列：下单时间、订/退单号、往来单位名称、联系人、联系电话、订货金额、运费金额、应收金额、已收金额、待收金额。
- 资金账户交易固定列：日期、账户名称、单据类型、单据号、往来单位、金额、余额；账户汇总固定列：账户名称、账户类型、期初余额、本期收入、本期支出、期末余额。
- 原文未给搜索条件。期间、Tab、稳定排序、分页、导出和 URL 只按 `DEC-FIN-066/070` 的最终结论实施，不自行添加图表、同比环比、二维表或任意公式。

### 表单与校验

本切片没有新增或编辑业务表单。唯一输入为报表 Tab、期间、分页和原文确认后的最小筛选；参数必须经 Query Schema 校验，未来期间、非法 report/page/pageSize 被拒绝或规范化，不进入 Repository。

机构来源不是页面下拉补填：一旦 provider 接入，收款确认命令需保存机构 ID/编码/名称快照。历史收款缺失机构时显示资料不可用并从机构聚合中明确排除，不能由 View 临时写“总部”。订单下单时间若需展示，应在应收形成快照中保存真实订单时间；缺失时显示 unavailable，不用发货/应收时间替代。

### 详情与操作矩阵

| 操作 | 前置 | 成功结果 | 失败/恢复 | 决策门 |
|---|---|---|---|---|
| 查看机构汇总/明细 | provider 可用且有权限 | 累计、本期、笔数和逐笔结果同源 | provider 失败只使机构页 unavailable | `DEC-FIN-064/065/070` |
| 切换统计报表/期间 | query 合法 | URL 可恢复，行和 totals 同一快照 | 旧响应丢弃，不显示上一查询合计 | `DEC-FIN-066/070` |
| 查看现金统计 | 有查看权限 | 四方式金额、笔数、占比守恒 | 0 分母不显示伪造百分比 | `DEC-FIN-067` |
| 查看订单支付 | 应收/贷项/核销可用 | 三卡片与订单行净额守恒 | 任一核心 provider 冲突则 error/unavailable | `DEC-FIN-068` |
| 查看账户交易/汇总 | FIN-003 账本与期间可用 | 明细余额与汇总期末一致 | 快照冲突不静默挑一个结果 | `DEC-FIN-069` |
| 导出 | 最终权限允许 | 当前筛选全量 UTF-8 CSV，与页面遮蔽/金额一致 | 查询失败不生成空文件 | `DEC-FIN-070` |

### 状态与计算

已确认口径：

```text
机构累计收款 = 该机构全部有效实际客户收款之和
机构本期收款 = 所选自然月内有效实际客户收款之和
机构笔数 = 匹配 CustomerReceipt.id 去重数

方式占比 = 该方式有效实收金额 / 四方式有效实收总额
0 分母 => unavailable

订单净应收 = 原应收 - active credit amount
订单净已收 = active writeoff(cash + discount) - credit 已结算部分
订单待收 = max(0, 订单净应收 - 订单净已收)

账户期末 = 期初 + period income - period expense
transfer-out 与 transfer-in 分账户展示，企业净变化为 0
```

现金统计不把余额支付、退款支出或其他收款混入客户收款。订单统计不把“有收款单”直接当成已收，仍以有效核销为准；贷项与退款不能通过删除原应收/原核销实现。账户统计使用每条 movement 保存的操作后余额和 FIN-003 期间快照交叉校验。

### 页面状态与 Mock

| 场景 | 本批次要求 |
|---|---|
| `normal` | 四张统计从同一 canonical Finance 状态返回可对账结果 |
| `empty` | 0 行与 0 合计语义明确；占比显示 unavailable 而非 NaN |
| `error` | 查询失败显示重试，不保留过期行/合计 |
| `slow` | 共享查询状态，旧响应不覆盖新期间/Tab |
| `permission-denied` | 路由/UI/Store/Service/导出一致拒绝 |
| `partial-failure` | 机构 provider 失败只影响机构页；订单增强资料失败保留资金事实并标记 partial |
| `unavailable` | 机构 owner 未接、缺失真实下单时间或账本冲突时明确不可用，不用 0/当前值代替 |
| `boundary` | 月初月末、已结转/未结转、0 分母、贷项超过结算边界可复现 |

只读统计不制造 `concurrent` 写冲突；一次 Repository 快照与过期响应测试覆盖其一致性风险。

### 验收追踪

| 验收 ID | 来源/决策 | 可观察结果 | 自动证据 | 人工证据 |
|---|---|---|---|---|
| `FIN005-A01` | §8、FIN-11、`DEC-FIN-064/065` | 机构快照、累计/本期/笔数和明细一致；缺 provider 不伪造总部 | provider/snapshot/aggregation tests | 机构列表→明细或 unavailable 说明 |
| `FIN005-A02` | §9.1、`DEC-FIN-066/067` | 四方式金额/笔数/占比与有效收款守恒 | method/dedup/zero-denominator tests | 方式统计与期间切换 |
| `FIN005-A03` | §9.2、`DEC-FIN-068` | 应收/已收/待收卡片与订单、贷项、核销净额守恒 | receivable-credit-writeoff tests | 订单支付明细与退单关联 |
| `FIN005-A04` | §9.3～§9.4、`DEC-FIN-069` | 交易明细操作后余额和账户汇总完全复用唯一账本 | ledger/period/transfer tests | 账户明细→汇总交叉核对 |
| `FIN005-A05` | 全局列表/权限、`DEC-FIN-070` | URL、分页、CSV、角色和脱敏一致 | query/export/permission tests | 代表性 1280px 四 Tab 页面 |
| `FIN005-A06` | 批次 L3 | 从零其他收支/转账进入统计，失败回滚且重启可复现 | shared Runtime/full verify | Mock reset 后黄金旅程和控制台检查 |
