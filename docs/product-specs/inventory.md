# 库存领域规格

## 需求来源与事实边界

- 直接业务来源：[05-库存模块.md](../references/requirements/05-库存模块.md)；全局交互与角色资料来自 [01-整体架构与首页.md](../references/requirements/01-整体架构与首页.md) §4～§5。
- 功能切片、章节、依赖、规则归属和字段准备度以 [产品规格索引](index.md) 为唯一目录。
- `INV-01～INV-16` 直接整理自原文 §8。“不变量”中的原子性、幂等 ID、整数分和可控时钟属于 Harness 工程约束，不新增业务状态。
- 原始需求只明确出库时扣库存，没有订单预占库存规则；库存 Service 不得自行增加预占、释放或超卖策略。加权成本细节和“超储”状态边界分别按 `DEC-INV-001`、`DEC-INV-002` 处理。

## 目标与范围

以仓库、库位、批次和库存流水为中心，支撑销售出库、采购入库、转仓、盘点、加工、分拣和配送。所有数量变化必须可追溯到单据。

## 主要页面

- 库存/批次库存查询、出入库明细。
- 仓库与库位。
- 转仓、其他出/入库、盘点、成本调整、库存结转。
- 加工配方、计划、加工单、领退料和出成率。
- 待出库、订单/波次分拣、配送任务/线路/车辆、标签。
- 进销存和出入库统计。

## 业务规则

| ID | 必须行为 |
|---|---|
| INV-01 | 确认出库时扣减可用库存。 |
| INV-02 | 确认入库时增加库存。 |
| INV-03 | 可用库存小于出库量时禁止出库。 |
| INV-04 | 批次默认按生产日期先进先出。 |
| INV-05 | 可用库存小于等于安全库存时预警。 |
| INV-06 | 盘点期间禁止相关出入库操作。 |
| INV-07 | 入库时按加权平均法重算成本。 |
| INV-08 | 转仓分别确认源仓出库和目标仓入库。 |
| INV-09 | 保质期商品距有效期不超过 30 天时预警。 |
| INV-10 | 实拣不等于应拣时生成差异单。 |
| INV-11 | 加工前维护成品与原料配比 BOM。 |
| INV-12 | 加工领料扣减原料库存。 |
| INV-13 | 加工完工入库增加成品库存。 |
| INV-14 | 已结转月份禁止出入库。 |
| INV-15 | 入库可指定库位并支持分拣定位。 |
| INV-16 | 波次合并多订单，先按商品汇总拣货再分播。 |

## 不变量

- 可用量不得为负；现存量、待出库量和可用量的来源与关系受 `DEC-INV-003` 约束，不能把“待出库”偷换成未定义的订单预占。
- 每次库存变更必须生成包含业务来源、仓库、库位/批次、数量、单位、方向和时间的流水；命令接口与来源要求受 `DEC-INV-012` 约束。
- 同一 command 重试不得重复扣增；原型使用 request ID 验证幂等属于工程约束，但不能代替业务单据来源。
- 成本金额使用整数分；计算精度、舍入和计入成本范围必须等待 `DEC-INV-001`，不能由代码局部决定。

## P0 验收

1. 销售出库和采购入库分别正确扣增库存并生成一条流水。
2. 库存不足、盘点锁定、月份结转三种情况均由 service 拒绝。
3. 多批次出库按 FIFO 分配；临期批次触发预警；超储状态在 `DEC-INV-002` 决定阈值后验收。
4. 转仓第一步只减少源仓在途增加，第二步才增加目标仓可用量。
5. 重复提交同一 request ID 不重复改变库存。
6. 重置 Mock 后全部库存与流水恢复基线。

## 原型模拟边界

打印、扫码、车辆调度与真实定位只做交互模拟；加工和波次在主流程稳定后进入 P1。

## INV-001 字段与交互契约

### 事实来源

- 直接页面事实：`05-库存模块.md §2～§3`；直接规则：`§8 INV-01～05/09/15`。
- 全局列表、详情、表单与角色资料：`01-整体架构与首页.md §4～§5`；细权限名称：`08-设置模块.md §3.2 四、库存模块权限`。
- 仓库存在跨文档补充：`08-设置模块.md §4.2` 明确仓库与库存模块共用数据，并增加仓库类型、禁售、地区和详细地址；冲突合并受 `DEC-INV-005` 约束。
- `PRD-001` 提供 SKU、基本单位、库存场景单位、换算率、保质期天数和管理生产日期。库存只通过商品公开 provider 读取，不能复制第二份商品资料。
- 订单的待出库和采购的在途数量尚无 passing 切片；本切片只能使用已确认的跨域快照契约，不能伪造已分配订单或采购事实。
- 原文 §2.3 的 Markdown 行把“仓库”误并入“单据类型”一格；按同一行的“操作仓库”说明恢复为独立第 3 列属于文档格式纠正，不新增字段。

### 页面与导航

库存工作区采用下列路由；路由命名是工程组织，不改变业务含义：

| 页面 | 路由 | 当前切片内容 | 非目标 |
|---|---|---|---|
| 库存列表 | `/inventory/stocks` | 仓库×SKU 库存、筛选、导出、库存详情入口 | 转仓/盘点按钮只跳转后续切片，不做无响应假按钮 |
| 库存详情 | `/inventory/stocks/:warehouseId/:skuId` | 数量摘要、SKU/单位、仓库、库位批次、相关流水 | 订单、采购或成本调整详情 |
| 批次库存 | `/inventory/batches` | 仓库/库位×SKU×批次余额与临期状态 | 新建批次；批次由未来入库命令产生 |
| 出入库明细 | `/inventory/movements` | 全部库存流水 | 关联单据详情未实现时显示来源编号及“对应模块未接入” |
| 批次出入明细 | `/inventory/batch-movements` | 带批次生产/有效日期的流水 | 修改或删除流水 |
| 仓库列表 | `/inventory/warehouses` | 查询、新增、编辑和状态 | 删除受 `DEC-INV-006` 约束 |
| 库位列表 | `/inventory/locations` | 左仓库导航、查询、导入导出、新增编辑 | 入库业务由 `PUR-002/INV-002` 消费 |

库存、批次、流水和仓库/库位共用二级导航。设置模块以后只通过库存公开 provider 复用仓库实体，不建立第二份仓库 Repository。

### 筛选与列表

| 页面 | 筛选 | 列表字段 | 查询与分页 |
|---|---|---|---|
| 库存列表 | 仓库、商品分类级联、库存状态、商品编码/名称/条码 | 仓库、SKU编码、商品名链接、规格、单位、期初、当前、待出库、可用、在途、安全库存、状态、成本单价、库存金额、操作 | 原文明示筛选；默认30条，仓库编码+SKU编码升序；金额口径受 `DEC-INV-001` |
| 批次库存 | 原文未定义；推荐仓库、批次状态、有效期至、商品/批次关键字 | 仓库、商品编码/名称、批次号、生产日期、有效期至、批次数量、成本单价/金额、入库时间、状态；实现时补库位列以满足 `INV-15` 追踪 | 推荐入库时间倒序、30条；受 `DEC-INV-010` |
| 出入库明细 | 原文未定义；推荐操作时间、仓库、单据类型、单据/SKU关键字 | 单据号/类型、仓库、SKU编码/名称、批次号、变动数量、变动后库存、成本单价/金额、操作时间/人 | 推荐操作时间倒序、30条；受 `DEC-INV-010` |
| 批次出入明细 | 同出入库明细，增加批次状态/关键字 | 同流水并增加生产日期、有效期 | 推荐操作时间倒序、30条；受 `DEC-INV-010` |
| 仓库列表 | 仓库名称/编码 | 名称链接、编码、状态、联系人、电话、地区+地址；设置原文另有类型/禁售 | 默认创建时间倒序、30条；合并列受 `DEC-INV-005/010` |
| 库位列表 | 仓库默认全部、库位编码/名称 | 仓库、库位编码/名称、当前存放商品、状态、备注 | 默认创建时间倒序、30条；商品由当前非零余额动态聚合，不在库位表单保存商品关系 |

库存和库位导出只导出当前查询结果，还是选中行优先，以及 CSV 字段/权限均未定义，统一受 `DEC-INV-011`。原文没有库存列表导入，不能自行增加。

### 表单与校验

#### 仓库

| 字段 | 原文事实 | 当前结论或阻塞 |
|---|---|---|
| 仓库编码 | 必填、系统唯一 | 唯一范围、长度、是否自动生成受 `DEC-INV-006` |
| 仓库名称 | 必填 | 唯一性与长度受 `DEC-INV-006` |
| 状态 | 必填，启用/禁用 | 默认与禁用引用处理受 `DEC-INV-006` |
| 联系人、联系电话 | 可空 | 原文没有格式/长度；仅做去空白和安全输入上限，电话不推断真实性 |
| 所在地区、详细地址 | 设置原文明示，库存原文只有“地址” | 采用哪套模型受 `DEC-INV-005` |
| 仓库类型、禁售仓库 | 仅设置原文明示 | 实体/虚拟/“仓位”冲突及禁售效果受 `DEC-INV-005` |
| 上级仓库 | 库位页声称仓库树有层级，但两份仓库表单均无字段 | 是否支持层级受 `DEC-INV-005`；未决定前不得补父级字段 |

#### 库位

| 字段 | 原文事实 | 当前结论或阻塞 |
|---|---|---|
| 仓库 | 必填 | 仅可选择允许新增库位的仓库；仓库禁用影响受 `DEC-INV-006/007` |
| 库位编码、名称 | 必填，编码唯一 | 唯一范围、长度、仓库是否可改受 `DEC-INV-007` |
| 状态 | 必填，启用/禁用 | 默认、非零库存时禁用/删除受 `DEC-INV-007` |
| 备注 | 可空 | 去空白；最大长度受 `DEC-INV-007` |
| 商品 | 只在列表展示 | 由该库位当前非零库存动态聚合，不是表单字段 |

库位导入推荐使用 UTF-8 CSV 并先预览逐行错误、任一错误整批拒绝；列、重复处理与导出范围受 `DEC-INV-011`，决定前不实现导入提交。

#### 安全库存

原文明示库存列表显示安全库存，设置权限又包含“设置安全库存”，但没有入口、维度和库存上限字段。推荐在库存行提供“库存阈值”短表单，以仓库×SKU 保存安全下限和可空上限；具体受 `DEC-INV-002`。

### 详情与操作矩阵

| 操作 | 角色/入口 | 前置与结果 | 阻塞或后续 |
|---|---|---|---|
| 查看库存/批次/流水 | 管理员、仓库人员；销售主管只读范围待定 | 不改变数据；商品 provider 失败时保留库存标识并显示资料不可用 | 权限与成本遮蔽受 `DEC-INV-013` |
| 导出库存/库位 | 原文明示 | fake CSV adapter，不含无权查看字段 | 格式和范围受 `DEC-INV-011/013` |
| 新增/编辑仓库 | 管理仓库权限 | 原子保存并记录变更日志；失败不改 Repository | 生命周期受 `DEC-INV-005/006` |
| 新增/编辑库位 | 管理库位权限 | 原子保存并记录变更日志 | 生命周期受 `DEC-INV-007` |
| 导入库位 | 导入库位权限 | 预览后原子提交，任一错误拒绝 | `DEC-INV-011` |
| 设置安全库存 | 设置安全库存权限 | 修改仓库×SKU 阈值并立即重算状态 | `DEC-INV-002` |
| 预览 FIFO 出库分配 | 未来订单/库存消费者 | 无副作用返回批次分配或不足/过期原因 | 批次规则受 `DEC-INV-008/009/012` |
| 确认入库/出库 | 仅 Service command，当前无页面入口 | 由明确业务来源调用，原子改余额、批次和流水，同 request ID 幂等 | 命令与来源受 `DEC-INV-012`；实际页面属后续切片 |

库存列表原文的“转仓/盘点”属于 `INV-002/003`。对应切片未实现前，页面显示清楚的规划边界或不呈现操作，不能放置无响应按钮。

### 状态与计算

- 数量行粒度推荐为企业×仓库×SKU；库位/批次余额汇总必须严格等于仓库 SKU 当前库存。规范库存单位、存储精度和显示换算受 `DEC-INV-004`。
- `当前库存` 是已确认入库减已确认出库的账面现存量；`待出库数量` 的订单来源、`可用库存=当前-待出库` 与 `在途数量` 的采购来源受 `DEC-INV-003`。
- `期初库存` 的“当期”、时区和快照来源未定义，受 `DEC-INV-010`。
- `安全库存` 规则明确为可用库存≤安全库存时预警；超储阈值和四状态优先级受 `DEC-INV-002`。
- 成本单价为只读快照；本切片不重算加权平均。库存金额、批次金额、负向流水金额和舍入受 `DEC-INV-001`。
- 批次状态明确包含正常/临期/过期；有效期当天边界、无有效期状态、批次唯一范围和 FIFO 同日排序受 `DEC-INV-008/009`。
- 过期批次禁止出库；库存不足必须由 Service 拒绝。是否要求所有库存必须在库位/批次中、非批次商品如何表达受 `DEC-INV-009`。
- 出入库流水一经写入不可编辑或删除；这是可追溯库存不变量。若业务需要冲销，必须由后续切片定义反向业务单据，不能修改历史流水。

### 页面状态与 Mock

| 场景 | 必须表现 |
|---|---|
| normal | 至少两个仓库、多个库位、上架/下架 SKU、正常/不足/零/临期/过期余额和可追溯流水；全部为明显虚构数据 |
| empty | 无余额、批次或流水时分别显示空状态；仓库为空时引导有管理权限者新增，不自动造默认仓 |
| error | 查询失败可重试；保存/导入失败不改状态；库存 command 原子回滚 |
| slow | 页面保持加载和禁重复提交，可安全离开只读列表 |
| permission-denied | 路由、UI、Service 三层一致；成本与仓库联系人不通过直接调用泄露 |
| partial-failure | 商品 provider 失败仍保留 SKU ID/余额，显示商品资料不可用；订单/采购 provider 未接入显示 unavailable 而非0 |
| boundary | 数量0/不足/恰好安全值/超上限、有效期昨天/今天/30/31天、同生产日批次、禁用/禁售仓、禁用库位、重复 request ID |

baseline 只保存 `INV-001` 自有仓库、库位、余额、批次、阈值与流水；商品只保存稳定引用。订单待出库和采购在途不得在库存 fixture 中冒充跨域订单/采购事实，除非 `DEC-INV-003` 明确允许使用带来源状态的模拟快照。

### 验收追踪

| 契约/规则 | 自动验证 | 人工验证 |
|---|---|---|
| 数量守恒与 `INV-01`、`INV-02`、`INV-03` | 仓库余额=库位/批次汇总、入出库原子性、不足拒绝、幂等 | 库存详情与流水能解释每次变化，不出现负可用量 |
| `INV-04`、`INV-09` | FIFO、多批次、同日排序、过期拒绝、临期端点 | 批次状态和拒绝原因可见，时间由模拟 clock 控制 |
| `INV-05` 与四状态 | 零/不足/正常/超储、阈值相等和冲突优先级 | 状态标签不仅靠颜色，修改阈值后可解释地变化 |
| `INV-15` | 库位归属、禁用引用、余额汇总、导入原子性 | 左右分栏、仓库切换、商品动态聚合和错误预览 |
| 权限与隐私 | 路由/UI/Service、成本遮蔽、导出字段 | 管理员/仓库/主管/拒绝角色逐一检查 |
| 七类场景与恢复 | normal/empty/error/slow/denied/partial/boundary、Mock 哈希 | 1280px、键盘、错误重试、脏表单、控制台、干净重启 |

黄金旅程设计：重置 baseline → 查看仓库 SKU 库存及详情 → 追溯库位、批次和流水 → 验证临期/过期状态 → 以同 request ID 重试模拟入库且只增加一次 → 预览 FIFO 出库并验证库存不足/过期拒绝 → 修改安全阈值观察状态 → 新增仓库和库位 → 预览并原子导入库位 → 切换角色验证成本与写操作边界。全部阻塞决策、实现和证据通过前不得宣称本旅程完成。

### 已确认决策

- `DEC-INV-001`：本切片只读取每基本单位的成本快照；金额=`基本单位数量×每基本单位成本` 后四舍五入到分。加权平均的费用组成和入库重算继续由 `PUR-002/INV-003` 决定，`INV-001` 不实现成本变更。
- `DEC-INV-002`：阈值按仓库×SKU；安全下限必填非负，上限可空且填写时必须大于下限。状态按可用量依次判定：`=0` 零库存、`0<可用≤下限` 不足、`上限非空且可用>上限` 超储、其余正常；禁售/禁用另做可用性标记，不混入库存状态。
- `DEC-INV-003`：当前库存由库存流水维护；待出库和在途由未来订单/采购 provider 提供。当前 provider 未接入时两列显示“数据源未接入”，可用库存也显示 unavailable，不用0代替；对外库存能力在此阶段只返回当前库存，直到订单分配规则确认后再开放可出库量。
- `DEC-INV-004`：所有余额和流水以基本单位精确存储，最多3位小数；页面按商品库存场景单位换算显示并同时保留基本单位提示。换算使用十进制定点计算，不使用浮点；FIFO 与不足校验统一转基本单位。
- `DEC-INV-005`：库存领域持有唯一仓库实体，合并设置原文的实体/虚拟类型、禁售、三级地区和详细地址；“仓位”不作为仓库类型，因为已有独立库位实体。仓库暂不支持父子级，库位页左侧“仓库树”按实体/虚拟分组展示平级仓库；设置页未来通过 provider 共用。
- `DEC-INV-006`：仓库编码和名称企业内唯一，手工输入、去空白、1～40字；默认启用。已有余额、库位或流水时不允许删除，本切片不提供删除；非零库存或启用库位存在时禁止禁用。禁售只排除未来销售出库候选，不影响采购入库和库存查询。
- `DEC-INV-007`：库位编码/名称在所属仓库内唯一、1～40字，备注≤500字，默认启用；已有余额或流水时不删除，本切片无删除。非零库存时禁止禁用或更换仓库；禁用库位保留历史但不可新入库。
- `DEC-INV-008`：批次号在企业×SKU内唯一，同一批次可分布多个仓库/库位；生产日期不得晚于入库日，有效期不得早于生产日期。当前日>有效期为过期；0～30天（含有效期当天和第30天）为临期；无有效期为正常。FIFO 按生产日期、入库时间、批次号升序，生产日期为空时排在有日期批次之后。
- `DEC-INV-009`：管理生产日期或设置保质期的商品入库必须带批次、生产日期；有保质期时有效期按生产日期+保质期天数计算并允许入库确认前人工纠正。其他商品可使用系统生成的非追踪批次；所有余额必须落到一个启用库位，仓库创建时不自动造默认库位。
- `DEC-INV-010`：期初库存按企业时区 Asia/Shanghai 当前自然月月初快照；本切片 baseline 明确保存快照，尚未结转时不反推历史。批次与流水采用上述最小筛选，所有列表30条分页；余额按仓库/SKU稳定排序，事件类按时间倒序；查询进入 URL。
- `DEC-INV-011`：库存/库位导出采用 UTF-8 CSV fake adapter，选中行优先否则当前全部筛选结果；库位导入列为 `warehouseCode,locationCode,locationName,status,note`，先逐行预览，重复行报错，任一错误整批拒绝；不导出成本或联系人给无相应权限角色。
- `DEC-INV-012`：库存 command 必须带 `requestId/sourceType/sourceId/operatorId/occurredAt`；入库另带仓库、库位、SKU、基本单位数量、成本快照和必要批次资料，出库先返回 FIFO 分配再原子确认。当前仅提供 Service 和自动测试，不建立手工入出库页面；业务来源不存在、仓库/库位不可用、过期或不足均整体拒绝。
- `DEC-INV-013`：超级管理员与仓库人员可查看库存、批次、流水和成本，并管理仓库/库位/阈值及导入导出；销售主管只读库存数量/状态，遮蔽成本、库存金额、仓库电话且不可导出；业务员和财务不可进入。路由、UI、Service 三层一致。

`DEC-INV-001～013` 已由用户在 2026-08-12 明确要求全部按推荐方案执行并写回；`INV-001` 达到 `ready`。这只开放当前切片实现门，不代表 `ORD-004/PUR-002/INV-002～006` 已有可执行契约。

### INV-001 实现状态

`INV-001` 已于 2026-08-12 完成：库存/详情/批次/流水/仓库/库位页面、基本单位定点余额、月初快照、成本只读快照、阈值、临期状态、FIFO 预览、幂等入出库 command、原子库位导入、CSV 导出、引用保护和三层权限均已有 Repository/Service/Runtime/UI 与自动证据。订单待出库和采购在途 provider 尚未实现，相关列及可用库存按契约显示 `unavailable`，没有以 0 伪造跨域事实；真实销售出库、采购入库和手工出入库页面仍分别属于 `ORD-004/PUR-002/INV-002`。证据见 `../exec-plans/completed/2026-08-12-inventory-foundation.md`。

## ORD-005 客户退货入库集成契约（ready）

订单原文要求客户退单审核通过后手工创建类型为“退货”的其他入库单并关联退单号；库存流水类型明确包含“客户退单入库”。`ORD-005` 可以消费 INV-001 已有幂等入库 command，但不能直接改余额，也不能借此实现盘盈、赠品和其他入库的 `INV-002` 通用页面。

退货入库命令至少需要 `requestId/sourceType=customer-return/sourceId/operatorId/occurredAt/warehouseId/locationId/skuId/quantityMilli/costPerBaseUnitCents` 以及追踪商品的批次、生产和有效期。成功必须追加不可变库存流水并向 Order 返回 movement 引用；Order 与 Inventory 写入必须原子，任何一行、仓库/库位、批次、版本或权限失败时全部不生效。

用户于 2026-08-14 确认全部推荐方案：审核后由仓库在退单仓库内选择启用库位，一次整单精确入库，不支持部分或差异收货；追踪商品沿用原出库批次、生产日期、有效期和原出库流水成本，非追踪商品使用系统批次；退款前可因 1～200 字原因整张精确作废并恢复待收货，退款后禁止作废。仅开放 `sourceType=customer-return` 的窄 public command，不借此开放 `INV-002` 通用页面。
