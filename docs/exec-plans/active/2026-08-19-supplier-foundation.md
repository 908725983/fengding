# PUR-004 供应商与直送供应

- 类型：business-feature
- 功能：PUR-004
- 当前阶段：implementation
- 状态：active
- 最近更新：2026-08-19

## 目标

把采购 §3～§4 的供应商主数据、供应商供货商品和直送供应统计整理为不依赖 AI 猜测的字段、关系粒度、生命周期、价格、权限、页面状态、Mock 和验收契约。先固定供应商与供货关系这一采购前置事实；直送统计缺少采购订单/退单与直送执行事实时必须明确 unavailable，不用演示数组伪造完成。

## 范围与非目标

范围包括供应商列表、详情、新增/编辑、启停，供应商供货商品的分类/搜索/新增/调价/停用，CSV fake 导入导出，五角色权限和八类页面状态；包括直送供应统计入口、来源边界及后续可编码契约。

采购补货、智能/快速采购、采购订单、入库、退单、应付和采购统计分别属于 `PUR-001/002/003/005`、`FIN-002`；本切片不得提前创建采购单、改变库存或形成应付。真实供应商账号、外部门户、短信、银行校验、物流和文件上传不在本切片内。

## 事实来源

- `docs/product-specs/procurement.md`：采购领域当前规则、跨域边界与验收入口。
- `docs/product-specs/products.md`：SKU、采购场景单位、分类和历史快照边界。
- `docs/product-specs/index.md`：`PUR-004` 来源/依赖/准备度及 `DEC-PUR-004～018` 唯一决策目录。
- `docs/references/requirements/04-采购模块.md §1.1、§3～§4、PUR-03、PUR-08`：供应商、供货商品和直送统计的直接业务来源。
- `docs/references/requirements/01-整体架构与首页.md §4～§5`：列表、详情、表单通则和五角色采购粗权限。
- `docs/references/requirements/08-设置模块.md §3.1.3 采购模块权限`：供应商查看/管理/导入/导出及供货商品查看/管理权限名。
- `ARCHITECTURE.md`、`docs/FRONTEND.md`、`docs/RELIABILITY.md`、`docs/SECURITY.md`：engineering-constraint；用于分层、确定性 Mock、页面状态、fake 外部动作和敏感数据边界，不新增供应商业务规则。

## 前置门禁

- `PUR-004` 是阶段三首个切片，索引未登记业务依赖；`SHELL-001`、`PRD-001` 等可复用工程/商品事实已有 passing 证据。
- 开工时 Git 工作树干净，active 目录只有 `.gitkeep`；本文件是唯一业务 active plan。
- 2026-08-19 运行 `./scripts/init.ps1`：Harness 45 个切片/182 个决策门、类型检查、59 个测试文件/272 项测试和生产构建通过；仅有既知 chunk size 提示。
- 用户于 2026-08-19 确认 `DEC-PUR-004～018` 全部采用推荐方案，决定已写回采购规格和产品索引，字段准备度达到 `ready`，允许进入实现但不代表完成。

## 验证路径

- specification：逐行核对采购 §3～§4、PUR-03/PUR-08、全局 §4～§5、设置采购权限和现有 Product 采购单位；运行 `npm run verify:harness` 与 `git diff --check`，确认没有业务代码变更。
- implementation：`Types → Schema → baseline → Repository → Service → Runtime/Store → UI → fake CSV`；供应商/关系数据只经 Repository，未来采购只经公开只读 provider。
- 自动验证：编码唯一与并发、字段校验、启停、供货关系粒度/价格/首选、历史快照、不满足 PUR-03 时拒绝、CSV 原子预览、权限遮蔽、直送 unavailable 和重置哈希。
- 页面状态：normal、empty、error、slow、permission-denied、partial-failure、boundary、concurrent；真实空值、0、masked 和 unavailable 不混用。
- 黄金旅程：重置 → 新建供应商 → 建立两个 SKU 供货关系 → 修改首选/供应价 → 产品侧 provider 可解释读取 → 停用关系/供应商后新采购不可选但历史保留 → 导入预览/导出 → 五角色 → 直送 unavailable → 故障与重置 → 1280px、键盘、控制台与干净重启。

## 风险与阻塞

- 列表把“启用 / 供货商品”写在同一个状态 Tab 中，状态筛选与子页面导航相互冲突。
- 供应商列表有“账号”，表单没有账号字段；不能据此创建登录凭据或密码。
- “购销/代销”是交易类型，不足以判断供应商能否入仓、直送或两者兼有。
- 供货商品只有“供应价”，未说明关联到商品还是 SKU、使用哪个单位、是否允许多个供应商和如何自动选供应商。
- 价格可编辑但无生效时间、历史、删除和既有采购单影响；直接覆盖会破坏未来采购追溯。
- 全局五角色没有采购员，采购模块却把仓库人员标为完整权限；银行账号和供应价的可见范围仍未定义。
- 直送统计没有直送事实标记、有效状态、正负口径或仓库语义，且 `PUR-002/003` 尚未实现；当前不能生成可信统计。

## 进度日志

- [x] 从总体顺序选择 `PUR-004`，确认 active 为空、Git 干净且基线通过。
- [x] 阅读 Harness 导航、计划、架构、质量、采购规格、采购 §3～§4、全局交互/角色和设置采购细权限。
- [x] 在采购规格提取页面、字段、关系、状态、权限、Mock 和验收候选，不创建业务代码。
- [x] 在唯一决策目录登记 `DEC-PUR-004～018`。
- [x] `npm run verify:harness` 通过：45 个功能切片、197 个决策门、8 份领域规格/需求快照；`git diff --check` 通过，`src/`、`mock/` 与 `package.json` 无差异。
- [x] 用户回复“PUR-004 全部按推荐方案执行”；15 项决定逐项写回采购规格、索引和本计划，字段准备度改为 ready。
- [ ] 当前步骤：运行 ready 门禁后，实现 Types、Schema、baseline、Repository 与 Service，建立领域核心检查点。

## 开放决策

| 决策 ID | 缺失事实与证据 | 可选方案及影响 | 阻塞范围 | 可安全继续 | 决定人 | 状态 | 结论与写回位置 |
|---|---|---|---|---|---|---|---|
| `DEC-PUR-004` | 本切片含直送统计但没有采购执行事实 | 推荐只实现供应商/关系，直送入口 unavailable；否则必须提前扩写三个切片 | `PUR-004` 完成范围 | 供应商字段目录 | 产品负责人/采购 | decided | 2026-08-19 用户确认推荐；写回 `procurement.md` 与索引。 |
| `DEC-PUR-005` | 编码/名称唯一与可修改性未定义 | 推荐编码企业唯一且不可改、名称唯一可改；放宽会增加导入和引用歧义 | `PUR-004` 主数据标识 | 页面布局 | 产品负责人/采购 | decided | 2026-08-19 用户确认推荐；写回 `procurement.md` 与索引。 |
| `DEC-PUR-006` | 交易类型不能表达直送能力 | 推荐另设入仓/直送/均可；复用购销/代销会混淆两个维度 | `PUR-004,PUR-002` 直送资格 | 普通联系人字段 | 产品负责人/采购 | decided | 2026-08-19 用户确认推荐；写回 `procurement.md` 与索引。 |
| `DEC-PUR-007` | 列表账号无表单或凭据规则 | 推荐账号 unavailable；创建外部账号会引入凭据和门户范围 | `PUR-004` 账号字段 | 其余主数据 | 产品负责人/管理员 | decided | 2026-08-19 用户确认推荐；写回 `procurement.md` 与索引。 |
| `DEC-PUR-008` | 启停/删除及引用副作用未定义 | 推荐可恢复启停、不删除；删除会破坏关系和历史追溯 | `PUR-004,PUR-001,PUR-002` 生命周期 | 只读查询 | 产品负责人/采购 | decided | 2026-08-19 用户确认推荐；写回 `procurement.md` 与索引。 |
| `DEC-PUR-009` | 校验、默认值和银行字段组合未定义 | 推荐固定长度、默认启用、银行两项成对；宽松保存会产生不可用资料 | `PUR-004` 表单/Schema | 列表字段 | 产品负责人/采购/财务 | decided | 2026-08-19 用户确认推荐；写回 `procurement.md` 与索引。 |
| `DEC-PUR-010` | 状态 Tab、详情和 URL 契约冲突 | 推荐列表/供货商品独立导航、三状态筛选和详情页；合并会导致语义冲突 | `PUR-004` 信息架构 | 实体字段 | 产品负责人/设计 | decided | 2026-08-19 用户确认推荐；写回 `procurement.md` 与索引。 |
| `DEC-PUR-011` | 供货关系粒度、单位和多供应商未定义 | 推荐供应商×SKU、采购单位价格、允许多供应商；SPU 粒度无法表达规格价格 | `PUR-004,PUR-001,PUR-002` 关系模型 | 分类/搜索 | 产品负责人/采购/商品管理员 | decided | 2026-08-19 用户确认推荐；写回 `procurement.md` 与索引。 |
| `DEC-PUR-012` | 自动选供应商与首选关系未定义 | 推荐每 SKU 一个启用首选；最低价自动选择会引入未定义策略 | `PUR-001,PUR-002` 自动分组 | 手工选择 | 产品负责人/采购 | decided | 2026-08-19 用户确认推荐；写回 `procurement.md` 与索引。 |
| `DEC-PUR-013` | 调价历史及订单回显未定义 | 推荐正价立即生效、版本审计、采购单保存快照；动态回算会改历史 | `PUR-004,PUR-002` 价格追溯 | 关系只读 | 产品负责人/采购/财务 | decided | 2026-08-19 用户确认推荐；写回 `procurement.md` 与索引。 |
| `DEC-PUR-014` | 关系停用/移除及引用影响未定义 | 推荐关系可恢复启停且历史不变；物理移除会丢失 PUR-03 证据 | `PUR-004,PUR-001,PUR-002` 关系生命周期 | 关系列表 | 产品负责人/采购 | decided | 2026-08-19 用户确认推荐；写回 `procurement.md` 与索引。 |
| `DEC-PUR-015` | 导入导出格式、范围和错误处理未定义 | 推荐 UTF-8 CSV fake、全量筛选导出、原子预览导入；部分成功难以重置 | `PUR-004` CSV | 手工维护 | 产品负责人/采购/管理员 | decided | 2026-08-19 用户确认推荐；写回 `procurement.md` 与索引。 |
| `DEC-PUR-016` | 五角色、数据范围及敏感字段未定义 | 推荐管理员/仓库管理且仓库遮蔽银行账号；新增采购员会越出五角色模型 | `PUR-004` 权限/隐私 | 管理员结构 | 产品负责人/管理员/仓库 | decided | 2026-08-19 用户确认推荐；写回 `procurement.md` 与索引。 |
| `DEC-PUR-017` | 直送事实、时间、正负和仓库列未定义 | 推荐未来有效入库正、退采出库负、客户为目的地；当前保持 unavailable | `PUR-004,PUR-002,PUR-003` 直送统计 | unavailable 页面 | 产品负责人/采购/仓库/财务 | decided | 2026-08-19 用户确认推荐；写回 `procurement.md` 与索引。 |
| `DEC-PUR-018` | 并发保存、审计和重置未定义 | 推荐乐观锁、幂等、脱敏日志和八场景重置；覆盖写会丢变更 | `PUR-004` 一致性/验收 | 单用户静态规格 | 产品负责人/技术负责人 | decided | 2026-08-19 用户确认推荐；写回 `procurement.md` 与索引。 |

上述 `DEC-PUR-004～018` 已于 2026-08-19 全部采用推荐方案，状态和逐项结论以 `docs/product-specs/index.md` 为唯一目录，完整行为写回 `docs/product-specs/procurement.md`。

## 中断恢复点

当前处于 `PUR-004` implementation：基线与 specification 已通过，`DEC-PUR-004～018` 已决定并写回，字段准备度为 ready；尚未新增采购代码、fixture、路由或页面。恢复时先核对本计划、索引和 Git 现场，下一步实现 Types、Schema、baseline、Repository 与 Service。
