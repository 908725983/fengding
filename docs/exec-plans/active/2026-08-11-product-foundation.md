# PRD-001 商品、SKU 与多单位

- 类型：business-feature
- 功能：PRD-001
- 当前阶段：specification
- 状态：active
- 最近更新：2026-08-11

## 目标

从原始需求提取商品列表、新增/编辑、SKU 多规格、四维单位和商品详情 Tab 1 的完整字段与交互契约；把所有缺失、冲突或多义事实登记为人工决策，全部写回并通过门禁后再决定是否进入实现。

## 范围与非目标

范围仅包括 `PRD-001`：商品 SPU/SKU 基础档案、商品列表的 SPU/SKU 展示切换、新增/编辑、规格变体、库存/采购/分销/销售单位和商品详情 Tab 1。`PRD-002` 价格体系与调价、`PRD-003` 商品授权、`PRD-004` 铺货与订单模板、`PRD-005` 辅助资料管理均为非目标；本切片可以引用它们提供的选项，但不得提前实现其管理行为或推测跨切片计算。

## 事实来源

- `docs/product-specs/products.md`：商品领域现有规则、边界与验收入口。
- `docs/product-specs/index.md`：`PRD-001` 来源、依赖、规则、字段准备度和人工决策目录。
- `docs/references/requirements/03-商品模块.md §2.1～§2.3 Tab 1、§6.3、§7`：商品列表、表单、SKU、多单位、详情和规则原文。
- `docs/references/requirements/01-整体架构与首页.md §4～§5`：全局列表、详情、表单和角色权限资料。
- `ARCHITECTURE.md`、`docs/FRONTEND.md`、`docs/RELIABILITY.md`、`docs/SECURITY.md`：engineering-constraint，仅约束分层、可测试性、视觉一致性与模拟安全，不新增业务事实。

## 前置门禁

- `SHELL-001` 已有 completed/passing 证据。
- `PRD-001` 当前为 `source-only`，只允许 specification，禁止写商品业务代码。
- `DEC-PRD-003～012` 均为 open；不受影响的规格已提取，但 `PRD-001` 整体尚未达到 ready。
- 开工基线必须通过 `./scripts/init.ps1`。

## 验证路径

- 对照 `03-商品模块.md §2.1～§2.3 Tab 1、§7` 与全局 `§4～§5`，逐项核对路由、筛选、列表、表单、详情、操作、状态、计算、Mock 和验收。
- `npm run verify:harness` 检查功能来源、精确章节、计划唯一性、决策登记和阶段门禁。
- 规格阶段不新增业务代码；使用 `git diff` 确认只修改产品规格、决策目录、计划与必要质量记录。
- 后续实现阶段必须覆盖 normal、empty、error、slow、permission-denied、部分失败、1280px 与键盘路径，并从重置基线执行完整黄金旅程。

## 风险与阻塞

- 原文同时描述 SPU 与 SKU 编码、价格字段、多种单位和辅助资料，但未完整定义归属、默认值、精度、状态转换、删除副作用和角色操作权限；已登记 `DEC-PRD-003～012`。
- `PRD-001` 与 `PRD-002/003/005` 的边界已写入契约：本切片只保存价格资料和引用辅助资料，不提前实现价格解析、授权或辅助资料生命周期。

## 进度日志

- [x] 按 Harness 顺序确认下一切片为 `PRD-001`，工作区干净且没有其他业务 active plan。
- [x] 读取仓库入口、总体顺序、计划规则、商品规格、指定原始需求与工程约束。
- [x] 完成页面、筛选、列表、表单、SKU、价格资料、多单位、详情、操作、状态、Mock 和验收契约提取。
- [x] 将九组新增缺口登记为 `DEC-PRD-004～012`，并补全既有 `DEC-PRD-003` 的阻塞范围。
- [x] `npm run verify:harness` 通过：44 个功能切片、42 个决策门、8 份领域规格和 8 份需求快照。
- [x] 规格检查点全仓 `npm run verify` 通过：Harness、类型检查、10 个测试文件/23 条测试和生产构建全部成功。
- [ ] 当前步骤：请用户集中确认 `DEC-PRD-003～012` 的推荐方案；收到决定后写回商品规格、索引和本计划，再运行 ready 门禁审计。

## 开放决策

| 决策 ID | 缺失事实与证据 | 可选方案及影响 | 阻塞范围 | 可安全继续 | 决定人 | 状态 | 结论与写回位置 |
|---|---|---|---|---|---|---|---|
| `DEC-PRD-003` | 基本单位、四维场景单位、换算方向/精度、数量小数位和价格换算未定义 | 推荐：SPU 级基本单位为锚点且换算率 1；四维单位默认基本单位，1 场景单位 = N 基本单位；换算率最多 6 位、数量最多 3 位，金额按比例换算后四舍五入到分 | 单位契约和后续数量/价格计算 | 商品其他基础资料 | 产品负责人/仓库/财务 | open | — |
| `DEC-PRD-004` | 商品名称、SPU/SKU 编码、简单/多规格模型、条码和简称规则不完整 | 推荐：将“商品标签”解释为商品名称，最多 80 字；简称最多 20 字不限制大小写；SPU/SKU 使用独立唯一命名空间 `SPU-000001`/`SKU-000001`，序号不复用；条码非必填但填入时 SKU 间唯一；简单商品自动一个默认 SKU，多规格必须填写规格名和值 | SPU/SKU 数据模型和唯一性 | 页面骨架 | 产品负责人/商品管理员 | open | — |
| `DEC-PRD-005` | 初始状态、转换、SKU 状态、删除方式和批量失败语义不完整 | 推荐：新增默认草稿；草稿可上架，上架可下架，下架可再上架；SKU 继承 SPU 状态；全部状态可编辑资料；仅草稿/下架且无订单引用可标记删除，编码不复用；批量命令全量预检、任一不合法则整体拒绝 | 状态机、删除、批量命令 | 只读状态 | 产品负责人/商品管理员/仓库 | open | — |
| `DEC-PRD-006` | SPU/SKU 列表、分类/标签/价格筛选和供应商依赖语义不完整 | 推荐：默认 SPU 视图；SPU 一行并以 `最低～最高` 聚合 SKU 价格，SKU 视图一行一个 SKU；分类含后代、标签任一命中、价格区间按基准订货价；关键字命中 SPU 或任一 SKU；供应商 provider 未接入时禁用并提示；详情进入 SPU，SKU 行编辑定位对应 SKU | 列表与查询 | 字段展示骨架 | 产品负责人/商品管理员 | open | — |
| `DEC-PRD-007` | 拼套、停用辅助资料和多组含混表单字段未定义 | 推荐：普通商品为默认；拼套商品本切片禁用并标记规划中；停用辅助资料只回显不可新选；“轮播库存”解释为轮播图；保质期为非负整数天；采购设置规划中；分类标签合并为商品标签；包邮默认否、运费模板可空、运费单位默认销售单位；只保留六个固定自定义文本字段，不支持动态扩展 | 表单模型和校验 | 普通商品核心资料 | 产品负责人/商品管理员/采购/物流 | open | — |
| `DEC-PRD-008` | 价格字段归属和最高/最低约束集合未定义 | 推荐：全部价格资料按 SKU 保存；金额非负、最多两位小数；最高/最低均可空，两者都有时最低≤最高；基准订货价和四级阶梯价必须落在已填写的上下限内，基准进货价不受售价上下限约束；本切片不做客户最终价解析 | 价格资料和后续价格服务输入 | 非价格资料 | 产品负责人/财务/销售主管 | open | — |
| `DEC-PRD-009` | 导入导出格式、冲突、范围和原子性未定义 | 推荐：原型使用 UTF-8 CSV fake adapter；导入先预览和逐行校验，整批原子提交，任一编码冲突则整批拒绝；导出有勾选时导出勾选项，否则导出当前筛选全部结果；媒体和富文本不进入 CSV | 导入导出和批量事务 | 手工新增与列表 | 产品负责人/商品管理员 | open | — |
| `DEC-PRD-010` | “只读/部分”角色的具体权限未定义 | 推荐：超级管理员完整读写；销售主管、业务员和仓库仅可看列表/详情，不能导出、导入、新增、编辑、状态变更或删除；财务不可进入；路由、UI、service 三层一致 | 权限和无权限场景 | 超级管理员路径 | 产品负责人/管理员 | open | — |
| `DEC-PRD-011` | 主图/视频/轮播和富文本安全边界冲突 | 推荐：主图与轮播图只接受 jpg/png/webp、单文件≤500KB、建议 1:1，最多 5 张；不支持 mp4 并明确规划中；原型只保存 fake 资源 ID；富文本采用安全白名单（文本格式、列表、链接、fake 图片），禁止原始 HTML 和自动请求外链 | 媒体、描述编辑与详情渲染 | 无媒体资料 | 产品负责人/设计/安全负责人 | open | — |
| `DEC-PRD-012` | 清空、保存、详情动作、日志和脏数据行为未定义 | 推荐：清空有脏数据时二次确认；保存回列表，保存并新增进入全新空表单；离开脏表单确认；教程为原型模拟；详情顶部按状态显示编辑/上架/下架/删除，右侧记录创建、编辑、状态和导入事件 | 表单动作、详情和日志 | 字段校验/只读内容 | 产品负责人 | open | — |

## 中断恢复点

当前处于 specification，尚未修改商品业务代码。字段与交互契约已写入 `docs/product-specs/products.md`，索引已登记 `DEC-PRD-004～012` 并扩充 `DEC-PRD-003`。开工基线为 Harness 通过、10 个测试文件/23 条测试、类型检查和生产构建成功。恢复时先核对 Git 与本计划，然后从当前步骤继续：取得用户对十组推荐方案的决定，逐条写回后运行 `npm run verify:harness`；任何 open 决策仍阻塞 `PRD-001` ready 和业务编码。
