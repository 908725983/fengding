# ORD-006 订单与销售统计

- 类型：business-feature
- 功能：ORD-006
- 当前阶段：specification
- 状态：active
- 最近更新：2026-08-17

## 目标

把订单 §6～§7 的九个统计报表入口整理为不依赖 AI 猜测的字段、事实、公式、单位、权限、页面状态、Mock 和验收契约。先用既有订单、退单、有效出库与退货入库建立七个可追溯报表；预售、套餐和其他出入库缺少事实源时明确 unavailable。只有全部阻塞决策写回且 `ORD-006` 达到 `ready` 后才开始统计代码。

## 范围与非目标

范围包括订单商品明细、退单商品明细、订单/退单按商品与下单单位汇总、按客户×商品汇总、销售出入库明细及按商品/客户汇总；包括筛选、URL 恢复、分页、合计、二维表设置、CSV fake 导出、权限和八类页面状态。

原文要求的预售两个 Tab 保留为需求可见但不可用候选，直到有预售订单、定金和转化契约；不以普通订单冒充预售。`INV-002` 其他出入库、真实 Excel、任意公式 BI、跨企业报表、生产级分析数据库、首页指标和独立数据分析模块不在本切片内。

## 事实来源

- `docs/product-specs/orders.md`：`ORD-001～005` 既有事实及本次 `ORD-006` 字段与交互契约。
- `docs/product-specs/products.md`：SKU、多单位、价格和历史商品快照边界。
- `docs/product-specs/inventory.md`：基本单位千分量、有效库存流水和 `INV-002` 未实现边界。
- `docs/product-specs/index.md`：`ORD-006` 来源/依赖/状态及 `DEC-ORD-052～066` 唯一决策目录。
- `docs/references/requirements/02-订单模块.md` 修订记录、§1.1、§6～§7、ORD-15：九报表名称、字段、筛选、合计、单位、正负方向和二维表的直接来源。
- `docs/references/requirements/01-整体架构与首页.md §4～§5`：列表、分页、空状态与五角色粗权限。
- `docs/references/requirements/08-设置模块.md §3.1.3`：订单统计查看、导出、明细及汇总细权限名称。
- `ARCHITECTURE.md`、`docs/FRONTEND.md`、`docs/RELIABILITY.md`、`docs/SECURITY.md`：engineering-constraint；用于分层、一致查询、无障碍、错误恢复、fake 输出和隐私，不新增统计口径。

## 前置门禁

- `ORD-006` 为 P1，直接依赖 `ORD-005`；`ORD-001～005`、`PRD-001`、`INV-001` 均已有 passing 证据。
- 开工时 Git 工作树干净，active 目录只有 `.gitkeep`；本文件创建后是唯一业务 active plan。
- 2026-08-17 运行 `./scripts/init.ps1`：Harness 45 个切片/167 个决策门、类型检查、57 个测试文件/257 项测试和生产构建通过；仅有既知 chunk size 警告。
- 当前 `ORD-006` 仍为 `source-only`，`DEC-ORD-052～066` 均为 open；本阶段不得新增统计业务代码或 fixture。

## 验证路径

- specification：逐行核对修订记录、§6～§7、ORD-15、设置统计权限和现有 Order/Return/Outbound/Inbound 字段；运行 `npm run verify:harness`、`git diff --check`，确认没有业务代码变更。
- implementation：`Types → Query/Schema → read-only Repository projection → Statistics Service → Runtime/Store → UI → fake export`；统计只能读取领域 Repository 的一致快照，不回写订单、库存或资金。
- 自动验证：候选状态、日期端点、历史快照、优惠分摊、待出/入库、发货率、单位换算、分组去重、正负移动、作废/冲销、二维表、分页/totals/CSV、权限和并发刷新。
- 页面状态：normal、empty、error、slow、permission-denied、partial-failure、boundary、concurrent；`0`、`—`、`unavailable` 必须语义不同。
- 黄金旅程：重置 → 订单明细筛选并复算优惠/赠品/部分出库 → 四种订单汇总守恒 → 退单待入库与完成数据 → 销售正出库/负退货且排除作废/冲销/短装 → 二维表与导出 → 五角色 → 故障与重置 → 1280px、键盘、控制台和干净重启。

## 风险与阻塞

- 原文修订记录与正文不同步：预售两页和蓝色说明只有摘要，没有完整字段与规则。
- 订单统计是“下单事实”，销售统计是“有效库存移动事实”；若不先固定状态与时间，两个报表会产生看似矛盾的数值。
- 商品净额需把订单级优惠稳定分到行，赠品和运费又有不同语义；错误口径会破坏明细/汇总守恒。
- 多单位既有下单单位、基本单位和辅助单位，原文又增加统计单位；缺少统一换算会导致数量和平均价错位。
- 套餐、物理编码、预售和其他出入库没有完整历史事实源；用空字符串或 0 会制造假数据。
- 设置只列订单统计细权限，没有销售统计角色映射；在 Service 前不固定会造成 UI 隐藏但接口泄露 totals。
- 并发出库/退货时若表格与 totals 分次读取，用户会看到同一页面两套版本。

## 进度日志

- [x] 从总体顺序选择 `ORD-006`，确认依赖 passing、Git 干净且 active 为空。
- [x] 运行 `./scripts/init.ps1`：Harness、类型检查、57 个测试文件/257 项测试和构建通过。
- [x] 阅读 Harness 导航、架构、计划、质量、前端、可靠性、安全、订单规格、原始 §6～§7、修订记录和设置权限。
- [x] 在 `orders.md` 写入九报表页面、字段、候选事实/公式、二维表、权限、八场景和验收契约；未创建业务代码。
- [x] 在唯一决策目录登记 `DEC-ORD-052～066`。
- [x] `npm run verify:harness` 通过：45 个功能切片、182 个决策门、8 份领域规格/需求快照；`git diff --check` 通过，`src/` 与 `mock/` 无差异。
- [ ] 当前步骤：由用户确认 `DEC-ORD-052～066` 的推荐方案；逐项写回 `orders.md`、索引和本计划，再复查 ready 门禁。
- [ ] 实现只读统计 Types/Schema、Repository 投影和公式 Service，建立第一个可回退代码检查点。
- [ ] 实现 Runtime/UI、二维表偏好与 fake CSV，建立第二个可回退检查点。
- [ ] 完成自动/浏览器/Mock 重置/干净重启验收，更新质量与总体顺序，归档计划。

## 开放决策

| 决策 ID | 缺失事实与证据 | 可选方案及影响 | 阻塞范围 | 可安全继续 | 决定人 | 状态 | 结论与写回位置 |
|---|---|---|---|---|---|---|---|
| `DEC-ORD-052` | 6 个订单统计 Tab 中预售两页只有修订摘要，蓝色说明正文缺失 | **推荐：保留两页入口并明确 unavailable，不造字段/数据；** 或隐藏入口；或把普通订单映射预售（高幻觉风险） | 页面范围、Mock、完成声明 | 七个有事实源报表 | 产品负责人/销售主管 | open | — |
| `DEC-ORD-053` | 订单/退单状态纳入及短装退款未定义 | **推荐：订单/普通退单均排除取消，其余已保存状态纳入并显示真实状态；短装不进退单商品统计；** 或只算已审核；或全状态含取消 | 候选事实、状态筛选、totals | 字段标签 | 产品负责人/销售主管/财务 | open | — |
| `DEC-ORD-054` | 默认日期、端点、时区和 URL 未定义 | **推荐：Asia/Shanghai 最近 3 个日历月，日期闭区间转 UTC 半开区间，查询全部进 URL；** 或无限期；或自然月 | query schema、日期验收 | 非时间字段 | 产品负责人/销售主管 | open | — |
| `DEC-ORD-055` | 历史报表是否随主数据修改 | **推荐：名称/编码/条码/规格/单位/价格用业务行快照，当前图片仅增强且失败降级；** 或全部当前值；或完全复制当前主数据 | 投影、分组键、历史回显 | ID/时间 | 产品负责人/商品管理员 | open | — |
| `DEC-ORD-056` | 金额、均价、原价、优惠、折扣率口径 | **推荐：商品净额=成交小计−可追溯券/整单优惠分摊，排除运费，赠品数量保留金额0；累计差额到分，0原价折扣率显示—；** 或不分摊订单优惠；或含运费 | 全金额和比率 | 文本/数量 | 产品负责人/财务/销售主管 | open | — |
| `DEC-ORD-057` | 待出/入库和发货率公式 | **推荐：只取当前有效且未作废/冲销数量，待数量=max(0,业务量−有效量)，发货率=有效出库/下单量并限制0～100%，0分母为—；** 或累计历史含作废；或按订单状态估计 | 履约指标 | 静态快照 | 产品负责人/仓库/销售主管 | open | — |
| `DEC-ORD-058` | 多单位与统计单位 | **推荐：计算先归一基本单位千分量；按商品显示基本单位，按下单单位用 SKU+保存单位分组；客户统计单位从该 SKU 已保存可换算单位选择，缺配置显示基本单位；** 或所有报表只用下单单位；或新增独立配置 | 数量、均价、分组 | 原始单位回显 | 产品负责人/商品管理员/财务 | open | — |
| `DEC-ORD-059` | 按客户粒度、单据数和 totals 范围 | **推荐：按客户保持客户×SKU×单位行；单据笔数对筛选后匹配行的单据 ID 去重；totals 统计全量筛选结果、分页不影响；** 或客户一行；或整单未匹配行也计金额 | 分组、合计、对账 | 明细列 | 产品负责人/销售主管 | open | — |
| `DEC-ORD-060` | 二维表允许项和保存方式 | **推荐：仅既有维度/指标、至少各一项，不允许公式；按 Mock 用户×报表保存，支持恢复默认且 reset 清除；** 或仅列显隐；或任意公式/跨域字段 | config/schema/UI | 固定默认表 | 产品负责人/销售主管 | open | — |
| `DEC-ORD-061` | 排序、分页、导出及“仅汇总”关系 | **推荐：明细时间倒序、汇总金额绝对值倒序并稳定 tie-break；30条分页；CSV 导出当前筛选/维度全部行；不选中、不打印/导入；“仅汇总”跳到按商品报表；** 或仅导当前页；或同页另算 | 查询、CSV、URL | 字段与公式 | 产品负责人/销售主管 | open | — |
| `DEC-ORD-062` | 销售统计权限与五角色范围 | **推荐：管理员/主管企业全量且可导；业务员仅本人且不导；仓库仅销售数量、金额遮蔽；财务企业全量只读且可导；五层一致；** 或所有有订单查看权者全量；或仅管理员 | 权限、隐私、导出 | 管理员结构 | 产品负责人/管理员/财务/仓库 | open | — |
| `DEC-ORD-063` | 物理编码、套餐、图片和空值来源不足 | **推荐：已有快照优先；真实空为—，缺 provider/未实现为“暂不可用”，真实0才显示0；物理编码/套餐不造数据，导出同语义；** 或全部空白；或从当前商品临时拼接 | 列、CSV、partial failure | 有快照字段 | 产品负责人/商品管理员 | open | — |
| `DEC-ORD-064` | “其他”移动、作废/冲销/短装和正负方向 | **推荐：本切片只算有效销售出库为正、有效普通退货入库为负；排除作废/冲销和短装；其他标明等待 INV-002；金额同号；** 或保留全部历史；或短装按负出库 | 销售统计事实 | 固定出库列 | 产品负责人/仓库/财务 | open | — |
| `DEC-ORD-065` | 刷新、并发版本与缓存 | **推荐：每次查询在一次 Repository read snapshot 中同时返回 rows/totals/version，不持久缓存；刷新重读，过期响应丢弃；** 或分接口最终一致；或保存物化报表 | 一致性、并发、恢复 | 固定 fixture 单线程 | 产品负责人/技术负责人 | open | — |
| `DEC-ORD-066` | 路由、默认页和视图切换 | **推荐：`/orders/statistics?report=...` 与 `/orders/sales-statistics?report=...`，默认各自明细；报表键/筛选/页码进 URL，两个一级页不共享日期；“仅汇总”切按商品键；** 或九条独立路由；或所有筛选全局共享 | 信息架构、恢复 | 独立字段契约 | 产品负责人/设计/销售主管 | open | — |

## 中断恢复点

当前停在 `ORD-006` specification：基线已通过，完整 source-only 契约和 15 项 open 决策已写入，未新增任何业务代码、fixture、路由或页面。恢复时先运行 `git status --short`，确认本计划是唯一 active，再核对 `orders.md` 的 `ORD-006` 章节与索引 `DEC-ORD-052～066`；下一步只能确认并写回决策，不能跳过 ready 门直接编码。
