# CUS-001 客户档案、分类与标签

- 类型：business-feature
- 功能：CUS-001
- 当前阶段：specification
- 状态：active
- 最近更新：2026-08-11

## 目标

从原始需求提取客户列表、详情、新增/编辑、客户分类、客户标签和客户智能标签的完整字段与交互契约；识别全部未定义或冲突事实，取得人工决定并写回后，才把 `CUS-001` 推进到 ready 和 implementation。

## 范围与非目标

范围仅包括 `CUS-001`：客户档案、客户分类、客户标签与 AI 标签确认。商机、常购商品、会员、公海、外勤、营销、企微、商城和分析属于后续切片；本阶段不编写业务代码、不创建业务 fixture、不生成客户页面。

## 事实来源

- `docs/product-specs/customers.md`：客户领域现有规则、边界与验收入口。
- `docs/product-specs/index.md`：`CUS-001` 来源、依赖、准备度和人工决策。
- `docs/references/requirements/06-客户模块.md §2.1、§2.4～§2.6`：客户档案、分类、标签与智能标签原始字段和操作。
- `docs/references/requirements/01-整体架构与首页.md §4～§5`：全局列表、详情、表单和角色权限资料。
- `docs/FRONTEND.md`、`docs/RELIABILITY.md`、`docs/SECURITY.md`：engineering-constraint，仅约束一致性、可测试性与模拟安全，不新增业务事实。

## 前置门禁

- `SHELL-001` 已 passing。
- `./scripts/init.ps1` 已通过：Harness、类型检查、2 个测试文件/3 个测试和生产构建成功。
- `CUS-001` 当前为 source-only，只允许 specification。
- 用户已于 2026-08-11 确认 `DEC-CUS-001`、`DEC-CUS-003～011` 的全部推荐方案并已写回。
- 写回审计新增 `DEC-CUS-012`；该项确认前不得把 `CUS-001` 标为 ready 或写业务代码。

## 验证路径

- 对照指定原文章节逐字段核对，确保页面、筛选、列表、表单、详情、状态、操作、Mock 和验收均有结论或明确决策 ID。
- `npm run verify:harness`：检查来源、章节、决策、active plan 和 ready 门禁。
- 规格完成且决策解决后，再规划 Types/Schema/Repository/Service/UI 的实现与分层测试；本阶段不执行这些实现验收。

## 风险与阻塞

- 十组核心缺口已经由用户确认推荐方案并写回产品规格。
- 首次推荐遗漏了少量必填选择项/状态默认值、账期字段联动、分类子级筛选和状态编辑条件；已集中登记 `DEC-CUS-012`，不能用控件默认行为代替产品决定。

## 进度日志

- [x] 读取 Harness 入口、总体顺序、客户规格和工程约束，确认基线通过。
- [x] 逐行提取 `06-客户模块.md §2.1、§2.4～§2.6` 与全局 §4～§5，形成字段、操作、页面状态、Mock 和验收契约。
- [x] 把全部未定义或冲突事实登记为 `DEC-CUS-001`、`DEC-CUS-003～011`，并限定各自阻塞范围。
- [x] 运行 `npm run verify:harness`：44 个切片、31 个决策门、8 份领域规格和 8 份需求快照通过。
- [x] 用户于 2026-08-11 回复“全部按推荐方案执行”；`DEC-CUS-001`、`DEC-CUS-003～011` 已写回客户规格和决策目录。
- [x] 写回审计发现推荐范围遗漏项，新增 `DEC-CUS-012`，保持 source-only 防止带猜测编码。
- [x] 决策写回后运行 `npm run verify:harness`：44 个切片、32 个决策门、8 份领域规格和 8 份需求快照通过。
- [ ] 当前步骤：取得 `DEC-CUS-012` 答复并写回。
- [ ] 决策写回后运行 Harness 门禁，把 `CUS-001` 从 source-only 提升为 ready。
- [ ] 进入 implementation，按 Types/Schema → Repository → Service → Runtime → UI → 自动与人工验收的顺序实现。

## 开放决策

| 决策 ID | 缺失事实与证据 | 可选方案及影响 | 阻塞范围 | 可安全继续 | 决定人 | 状态 | 结论与写回位置 |
|---|---|---|---|---|---|---|---|
| `DEC-CUS-001` | 状态、下单影响和额度超限结果 | 推荐方案 | `CUS-001` 状态、`ORD-002` 校验 | 其他资料 | 产品负责人/财务 | decided | 2026-08-11 用户确认；后台新增启用、自助注册待审核、仅启用可下单、停用可恢复、冻结需原因、正额度超限阻止。已写回 `customers.md`。 |
| `DEC-CUS-003` | 自动编码格式和冲突 | 推荐方案 | `CUS-001` 编码 | 手工字段 | 产品负责人 | decided | 2026-08-11 用户确认；`CUS-000001` 企业内递增不复用，手工冲突拒绝，Mock 可重置。已写回 `customers.md`。 |
| `DEC-CUS-004` | 详情结构和 §2.1.3 语义 | 推荐方案 | `CUS-001` 详情 | 列表/表单 | 产品负责人 | decided | 2026-08-11 用户确认；二级导航、分组只读详情、顶部操作、右侧状态日志。已写回 `customers.md`。 |
| `DEC-CUS-005` | 分类生命周期和默认值应用 | 推荐方案 | `CUS-001` 分类命令 | 分类展示 | 产品负责人/销售主管 | decided | 2026-08-11 用户确认；无限层防循环、停用保留关系、引用时禁删、默认值只在新建选中时带入。已写回 `customers.md`。 |
| `DEC-CUS-006` | 标签颜色、生命周期和筛选 | 推荐方案 | `CUS-001` 标签命令 | 标签展示 | 产品负责人/销售主管 | decided | 2026-08-11 用户确认；五种颜色、停用保留关系、引用时禁删、使用后类型不改、多标签任一命中。已写回 `customers.md`。 |
| `DEC-CUS-007` | AI 建议处理与调度 | 推荐方案 | `CUS-001` AI 状态机 | 只读结果 | 产品负责人/销售主管 | decided | 2026-08-11 用户确认；待确认建议、逐条/批量确认拒绝、确认才改标签、自动计数≤1000、定时分析规划中。已写回 `customers.md`。 |
| `DEC-CUS-008` | 五个业务开关默认值与自动分配 | 推荐方案 | `CUS-001` 默认值/副作用 | 字段展示 | 产品负责人/销售主管 | decided | 2026-08-11 用户确认；五项默认 false、业务员手选、不执行自动分配。已写回 `customers.md`。 |
| `DEC-CUS-009` | 敏感字段权限 | 推荐方案 | `CUS-001` 查看/编辑/导出 | 非敏感资料 | 产品负责人/管理员 | decided | 2026-08-11 用户确认；管理员完整，销售可看电话但银行账号/税号遮蔽不可导出，仓库/财务不可进入。已写回 `customers.md`。 |
| `DEC-CUS-010` | 清空与保存动作 | 推荐方案 | `CUS-001` 表单动作 | 字段校验 | 产品负责人 | decided | 2026-08-11 用户确认；清空表单二次确认，保存回列表，保存并新增打开空白页，教程模拟。已写回 `customers.md`。 |
| `DEC-CUS-011` | 动态属性范围 | 推荐方案 | `CUS-001` 动态字段 | 六个预置字段 | 产品负责人/管理员 | decided | 2026-08-11 用户确认；只实现六个预置字段，动态字段另拆。已写回 `customers.md`。 |
| `DEC-CUS-012` | 必填选择项与结算默认、账期清理、分类/标签初始状态、标签类型/颜色、分类子级筛选及各状态编辑权限仍无事实 | 推荐：客户分类/业务员初始为空并强制选择，结算默认现结；切离账期结算时清空账期天数；分类和标签默认启用，标签默认手动/蓝色；分类筛选包含全部后代；四种客户状态均可编辑资料，但状态动作仍受状态机限制 | `CUS-001` 初值、联动、筛选和编辑条件 | 其余已确认契约 | 产品负责人 | open | — |

## 中断恢复点

已完成原始需求提取并写回用户确认的十组推荐方案。恢复时先读取本计划、`customers.md` 和产品规格索引；当前只等待 `DEC-CUS-012`。确认后写回初值、账期联动、分类筛选和编辑条件，运行 `npm run verify:harness`，通过后把 `CUS-001` 改为 ready、计划阶段改为 implementation，再从 Types/Schema 开始。未确认前不得编写客户业务代码。最近可回退规格检查点是提交 `047d0dc`；本次决定写回将在验证后形成新的 Git 检查点。
