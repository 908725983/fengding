# PRD-002 价格体系与调价

- 类型：business-feature
- 功能：PRD-002
- 当前阶段：implementation
- 状态：active
- 最近更新：2026-08-11

## 目标

从原始需求提取等级价格、进价调价、客户调价、历史调价、批量/自动调价与价格解析的完整字段、状态、计算和验收契约；缺失、冲突或多义事实必须登记决策并写回，达到 ready 前不写业务代码。

## 范围与非目标

范围仅包括 `PRD-002`：价格资料维护、调价单、生效历史、价格解析及 `PRD-01` 已提供的 SKU 价格输入。商品授权属于 `PRD-003`，促销/优惠券/会员权益属于客户后续切片，订单手工改价属于 `ORD-002/003`，不得提前实现或猜测组合结果。

## 事实来源

- `docs/product-specs/products.md`：商品领域现有规则、边界和验收入口。
- `docs/product-specs/index.md`：`PRD-002` 来源、依赖、规则、准备度与人工决策目录。
- `docs/references/requirements/03-商品模块.md §3、§7 PRD-02/04/09/17`：价格页面、字段、调价流程和规则原文。
- `docs/references/requirements/06-客户模块.md §2.4、§2.7、§4.1～§4.2`：客户分类折扣、会员折扣、优惠券和促销的跨切片冲突证据。
- `docs/references/requirements/01-整体架构与首页.md §4～§5`：全局列表、表单、详情和角色权限资料。
- `ARCHITECTURE.md`、`docs/FRONTEND.md`、`docs/RELIABILITY.md`、`docs/SECURITY.md`：engineering-constraint，仅约束分层、可测试性、确定性和模拟安全。

## 前置门禁

- `CUS-001`、`PRD-001` 已有 completed/passing 证据。
- `PRD-002` 已达到 `ready`，`DEC-PRD-013～022` 已决定并写回。
- `DEC-PRD-001` 继续阻塞后续最终成交价，但不阻塞本切片明确的三层预营销价格解析。
- 开工基线必须通过 `./scripts/init.ps1` 与 `npm run verify:harness`。

## 验证路径

- 对照精确原文逐项核对页面、字段、筛选、状态、生效时点、价格计算、权限、Mock 与验收。
- `npm run verify:harness` 检查来源、章节、计划唯一性、决策登记和阶段门。
- specification 阶段用 `git diff` 确认没有新增 `PRD-002` 业务代码。
- 后续 verification 必须覆盖 normal、empty、error、slow、permission-denied、部分失败、模拟时钟边界、1280px、键盘路径、Mock 重置和干净启动。

## 风险与阻塞

- 价格优先级只覆盖客户价、等级价和基准订货价，与分类折扣、会员折扣、促销及优惠券的关系未定义，受 `DEC-PRD-001` 阻塞。
- 订单关联 provider 尚未由 `ORD` 切片提供；按已确认边界只能显示“数据源未接入”，不得制造订单。
- 销售主管、业务员和仓库的敏感价格/客户身份遮蔽已经在 Service 执行，后续 Runtime、路由和 UI 仍必须重复执行同一权限矩阵。

## 进度日志

- [x] `PRD-001` 完成归档且工作区干净；按严格顺序选择 `PRD-002`。
- [x] 建立唯一 specification active plan，明确来源、依赖与禁止提前编码边界。
- [x] 逐项读取商品需求 §3、规则 PRD-02/04/09/17，以及客户分类/会员/促销/优惠券冲突来源。
- [x] 在 `products.md` 写入完整 `PRD-002` 页面、字段、状态/计算、Mock 与验收契约；未定义项没有用惯例补齐。
- [x] 新增 `DEC-PRD-013～022`，覆盖价格字段模型、状态、生效冲突、单位价格、客户状态、批量公式、历史、自动策略和权限。
- [x] 用户于 2026-08-11 要求“全部按推荐方案执行”；`DEC-PRD-013～022` 已逐项决定并写回，`DEC-PRD-001` 按推荐缩小为后续最终成交价决策。
- [x] `PRD-002` 字段准备度更新为 ready；价格模型、时钟、范围、单位、冲突、客户状态、批量、历史、策略和权限均有可执行结论。
- [x] 开工基线 `./scripts/init.ps1` 通过：Harness、类型检查、18 个测试文件/45 条测试和生产构建均成功。
- [x] 完成 Price Types、可执行 Schema、`featureData.PRD-002` baseline、事务 Repository、Mock Catalog/session、可控时钟与核心 Service。
- [x] Service 已覆盖三层价格解析、调价状态机、同刻冲突与回滚、逐字段版本历史、单位覆盖、自动策略、角色权限及敏感字段遮蔽；没有扩写最终营销成交价。
- [x] 全仓 `npm run verify` 通过：Harness、类型检查、20 个测试文件/59 条测试和生产构建；`mock:reset` 后 baseline/work SHA-256 均为 `BEB9285FCAEBFA723072BB675DC12D20B08978B3959092193F22E580D902882A`。
- [x] 价格领域核心检查点已提交：`606d6fb`。
- [ ] 当前步骤：实现 Price Runtime、七类可复现场景、价格子模块导航，以及等级/进价/客户调价单和历史调价的只读列表；本检查点不提前实现新增/编辑表单、单位价格页和自动策略页。

## 开放决策

| 决策 ID | 缺失事实与证据 | 可选方案及影响 | 阻塞范围 | 可安全继续 | 决定人 | 状态 | 结论与写回位置 |
|---|---|---|---|---|---|---|---|
| `DEC-PRD-001` | 后续营销价格组合未定义 | 推荐当前切片只返回三层预营销价格 | `CUS-003/CUS-006/ORD-002` 最终价 | `PRD-002` 全部范围 | 产品负责人 | open | 2026-08-11 按推荐缩小阻塞范围；不在价格基础切片猜测后续营销组合。 |
| `DEC-PRD-013`、`DEC-PRD-014`、`DEC-PRD-015`、`DEC-PRD-016`、`DEC-PRD-017`、`DEC-PRD-018`、`DEC-PRD-019`、`DEC-PRD-020`、`DEC-PRD-021`、`DEC-PRD-022` | 价格字段、调价状态/范围/冲突、单位覆盖、客户状态、批量公式、历史、自动策略和权限缺少执行事实 | 推荐方案 | `PRD-002` 对应实现范围 | 不适用 | 产品负责人/财务/销售主管/管理员 | decided | 2026-08-11 用户要求全部按推荐方案执行；逐项结论已写回 `products.md` 和规格索引。 |

## 中断恢复点

当前处于 implementation。规格/决策检查点为 `cca447e`，价格领域核心检查点为 `606d6fb`，该提交可独立回退；工作区应只包含本计划记录更新。恢复时从 `606d6fb` 后继续当前步骤：先接入 Price Runtime 与七类场景，再完成价格导航和四类只读列表，不先写表单、单位价格或自动策略页面。`DEC-PRD-001` 只阻塞后续营销最终价，不得在本切片扩写。
