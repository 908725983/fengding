# 对齐 OpenAI 高级 Harness 结构

- 状态：completed
- 完成日期：2026-08-11

## 目标

按照指南第 101—102 页的高级结构重新组织 Harness，使每类事实只有一个推荐入口，删除额外发明的治理层。

## 范围与非目标

范围包括目录迁移、有效内容归并、短导航重写和机械验证更新。不修改现有产品界面，不实现任何业务功能，也不删除八份领域产品规格和需求快照。

## 事实来源

- `learn-harness-engineering-guide-zh.pdf` 打印页 101—132，重点为 101—102 的目录与随后各文件职责。
- 当前仓库内已验证的 Harness、产品规格、需求快照和应用壳。
- 用户要求：删去偏离推荐结构、由助手自行增加的重复入口。

## 验证路径

- `npm run verify:harness`：通过；高级目录完整，8 份领域规格、8 份需求快照、45 行入口导航。
- `npm run typecheck`：通过。
- `npm run test:run`：2 个测试文件、3 个测试通过。
- `npm run build`：通过，Vite 生产构建成功。
- 结构检查会拒绝旧的自定义治理入口重新出现。

## 风险与阻塞

- 已把旧文件中的有效信息迁移到产品规格、设计文档、计划、质量、可靠性或安全入口。
- PDF 示例中的 Nixpacks、uv、新用户引导文件与本产品技术栈无关，未创建无内容的占位文件。
- 本次只调整 Harness，不提高任何业务领域质量评分。

## 进度日志

- [x] 重新读取指南并确认打印页与 PDF 文件页码映射。
- [x] 建立旧文件到推荐职责的迁移映射。
- [x] 创建 `design-docs/`、`generated/` 和技术债台账。
- [x] 把路线、开放问题、Mock、UI 字段和状态职责归入推荐入口。
- [x] 更新机械验证、清理旧入口并运行全部回归。

## 开放决策

无。与示例项目技术栈绑定的参考文件不适用于 Vue + Mock 原型，因此只保留本项目真实使用的需求与设计参考。

## 中断恢复点

本计划已完成。当前没有 active plan；下一业务切片是客户档案、分类与标签，开始时按 `docs/PLANS.md` 创建新计划。
