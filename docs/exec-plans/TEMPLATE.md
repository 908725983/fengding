# <功能编号> <功能名称>执行计划

- 创建日期：YYYY-MM-DD
- 关联功能：FEATURE-ID
- 所属路线图阶段：阶段 N
- 状态：执行中

## 目标

说明本计划结束时用户可以完成什么业务任务。

## 非目标

列出本次不实现的相邻功能，防止范围漂移。

## 事实来源

- 对应 `docs/product-specs/<domain>.md`
- 相关业务规则 ID
- 受影响的架构、前端和 Mock 文档
- 本功能在 `feature_list.json` 登记的 `ui_spec`

## 前置依赖

列出必须已 passing 的功能；如果未满足，不得通过复制数据绕过。

## 影响范围

列出预计新增或修改的领域目录、共享能力、Mock 契约和文档。

## 里程碑

- [ ] 从原始需求提取页面字段、按钮、状态与权限，完成 `ui_spec` 并登记开放问题。
- [ ] 建立类型、Repository 和 Mock 场景。
- [ ] 实现业务 Service 与单元测试。
- [ ] 实现 Store/Composable 和页面交互。
- [ ] 覆盖正常、空、错误、慢响应或权限场景。
- [ ] 完成自动验证与人工黄金旅程。
- [ ] 更新状态、证据和进度。

## 验收命令

```powershell
npm run verify
```

补充本功能的聚焦测试命令。

## 人工黄金旅程

以编号步骤说明输入、操作、预期状态、金额/数量变化和关联记录。

## 决策记录

按日期记录非显然选择、依据和影响。需求缺口不能在这里被假设成已确认规则。

## 风险与回退

说明失败影响、Mock 恢复方式和代码回退边界。

## 中断恢复点

- 更新时间：YYYY-MM-DD HH:mm
- 最后完成：尚未开始
- 当前代码状态：干净基线
- 下一条具体动作：填写第一项可执行动作
- 已修改文件：无
- 最近验证：未运行
- 未决问题：无

每次任务可能中断前更新本节；内容必须让未读取聊天记录的下一位开发者可以直接继续。
