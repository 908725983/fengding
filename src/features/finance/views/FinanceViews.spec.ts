import{beforeEach,describe,expect,it}from'vitest';import{mount}from'@vue/test-utils';import{createPinia,setActivePinia}from'pinia';import{createMemoryHistory,createRouter}from'vue-router';import FinanceAccountListView from'./FinanceAccountListView.vue';import FinancePaymentChannelView from'./FinancePaymentChannelView.vue';import FinanceReceivableView from'./FinanceReceivableView.vue';import FinanceReceiptListView from'./FinanceReceiptListView.vue';import FinanceWriteoffListView from'./FinanceWriteoffListView.vue';
async function mountView(component:object,path:string,props:Record<string,unknown>={}){const pinia=createPinia();setActivePinia(pinia);const router=createRouter({history:createMemoryHistory(),routes:[{path,component},{path:'/orders/:orderId',component:{template:'<div/>'}},{path:'/finance/:pathMatch(.*)*',component:{template:'<div/>'}}]});await router.push(path);await router.isReady();const wrapper=mount(component,{props,global:{plugins:[pinia,router]}});await new Promise(r=>setTimeout(r,260));await wrapper.vm.$nextTick();return wrapper}
describe('finance views',()=>{beforeEach(()=>setActivePinia(createPinia()));it('renders the complete account balance columns and deterministic rows',async()=>{const wrapper=await mountView(FinanceAccountListView,'/finance/accounts');expect(wrapper.text()).toContain('期初余额');expect(wrapper.text()).toContain('本期收入');expect(wrapper.text()).toContain('演示银行基本户');expect(wrapper.text()).toContain('¥5,650.00')});it('labels payment actions as prototype simulation',async()=>{const wrapper=await mountView(FinancePaymentChannelView,'/finance/payment-channels');expect(wrapper.text()).toContain('原型边界');expect(wrapper.text()).toContain('微企付');expect(wrapper.text()).toContain('不发网络请求')});it('renders customer and document receivable projections with conserved totals',async()=>{const customer=await mountView(FinanceReceivableView,'/finance/receivables',{mode:'customers'});expect(customer.text()).toContain('客户应收');expect(customer.text()).toContain('演示客户甲');expect(customer.text()).toContain('¥36.00');const documents=await mountView(FinanceReceivableView,'/finance/receivables/documents',{mode:'documents'});expect(documents.text()).toContain('YS-260731-00001');expect(documents.text()).toContain('部分收款')});it('renders traceable receipt and writeoff rows',async()=>{const receipts=await mountView(FinanceReceiptListView,'/finance/receipts');expect(receipts.text()).toContain('SK-260806-00001');expect(receipts.text()).toContain('可用预收');const writeoffs=await mountView(FinanceWriteoffListView,'/finance/writeoffs');expect(writeoffs.text()).toContain('HX-260806-00002');expect(writeoffs.text()).toContain('已取消')})})
